# Статус проєкту (оновлено 2026-09-12)

Коротка «пам'ять» про пророблене й що лишилось. Деталі RCTL — у `RCTL_COMPAT.md`,
плани фаз — у `PLANS.md`, посібник — у `USAGE.md`.

## Зроблено й перевірено на залізі

**SMS (зашифрована/чиста), обидва напрямки.** Формат — реверснута, валідована в ефірі
стокова схема TYT: AES-256-ECB над IPv4/UDP/TMS, порт 4007. Два режими: з ключем шифрує,
без ключа — чистий текст (сумісно зі стоковою). Адресний фільтр вхідних + перемикач монітора.
- **TX (форк→стокова):** Unconfirmed rate-1/2; заводська рація розшифровує наш TX. ✅
- **RX (стокова→форк):** стокова шле **CONFIRMED rate-3/4** (18-байтний блок = 2 байти
  DBSN+CRC9 + 16 байтів навантаження); форк пропускає 2 службові байти/блок, збирає PDU,
  CRC32 сходиться, показує текст. Перевірено на залізі 2026-09-05 (pdu/msg росли). ✅
- **Квитанція на вхідне CONFIRMED SMS (link-layer ACK) — реалізовано, хостовий тест
  (2026-09-12).** Формат знято з ефіру HackRF-ом (BBD_0005: стокова 0x26EA3D квитує RT4D
  0x26EA1B) і декодовано власним декодером `tools/dmr_data_rx.py`. Коли форк отримав усі
  блоки адресованого йому приватного CONFIRMED повідомлення (біт A=1, CRC32 PDU зійшовся),
  він відповідає **Response data header (DPF=1)**:
  `01 40 <кому:початк.відправник> <від кого:ми> 00 <BF> <CRC16^0xCCCC>` — SAP=4 (IP),
  Class=00 (ACK). Квитуємо ДО розшифровки (це про доставку кадрів, не про читання), тільки
  приватні й адресовані нам; групові й «моніторні»/чужі — ні.
  **Польова перевірка 1 (2026-09-12): квитанції нема, стокова шле повідомлення 3 рази
  (форк отримує всі три).** Тобто прийом справний, а квитанція не долітає. Виправлено й
  озброєно лічильниками (ітерація 2):
  - Ключування більше не по фіксованій паузі від завершення PDU, а **по ТИШІ в каналі**:
    чекаємо ≥60 мс без жодного data-burst (`s_lastRxBurstMs` оновлюється в
    `dmrSmsRxDiagBurst`, який кличеться на кожен data-sync burst). Причина: `dmrDataTxLoad()`
    сам відкладає ключування ще на 100 мс, тож стара схема влучала у хвіст/термінатор
    відправника — а він у цей момент ще передає і нас не чує. Дедлайн 2.5 с, далі кидаємо.
  - На `slotState` навмисно НЕ чекаємо (він тримається в RX-стані до END_TICK_TIMEOUT);
    детерміноване ключування робить сам `dmrDataTxLoad()` через `HRC6000ForceDMRIdleForTx()`.
  - **Лічильники `dmrSmsAckDiag()`** у хвості USB 0x93 (старі парсери цілі), читаються
    `tools/sms_diag.py`: бачив / вчергу / вефір / кинуто + **перші два байти останнього
    вхідного data-заголовка** + груповий?/нам?. Скрипт друкує готовий діагноз: чи стокова
    взагалі просить квитанцію (DPF=3 + біт A), чи відсіяв фільтр, чи не звільнявся канал.
  **Польова перевірка 2 (2026-09-12): лічильники показали `бачив=3 вчергу=3 вефір=3
  кинуто=0`, заголовок стокової `43 4c` (DPF=3, A=1, SAP=4) — тобто квитанція ЙШЛА в ефір
  усі 3 рази, а стокова однаково ретрансмітила.** Отже справа була не в таймінгу й не в
  детекції, а у ФОРМІ. Знайдено (ітерація 3): я переплутав призначення двох останніх байтів
  Response-заголовка. Правильно (як в ефірі):
  **`o8 = Blocks-to-Follow = 0x00`, `o9 = Class/Type/Status = 0x08`** (Class=00 ACK,
  Type=001) — ОБИДВА константи. Реалізація ж трактувала їх навпаки й підставляла в `o9`
  лічильник прийнятих блоків (у полі — 4), тобто слала `00 04` = Status=4 замість ACK.
  Хостовий тест цього не спіймав, бо приймав `bf` параметром і викликався саме з `0x08` —
  збіг з ефіром був випадковий. Тепер параметра немає, байти прибиті константами + окрема
  регресійна перевірка `o8/o9 == 00 08`.
  **Польова перевірка 3 (2026-09-12): `бачив=6 вчергу=4 вефір=4 кинуто=0`, заголовок `43 40`.**
  Квитанція знову йшла в ефір. Ітерація 5 — приводимо ПОДАЧУ до перевіреного зразка:
  **3 повтори заголовка-квитанції, БЕЗ CSBK-преамбул** (рівно як `dmr_rctl_stock_build_ack_tx`,
  чия квитанція з цією ж стоковою працює на залізі; команди RCTL шлються з 16 преамбулами, а
  квитанції — ні). Раніше було 2 преамбули + заголовок, тобто сам заголовок виходив на ~120 мс
  пізніше, а в ефірному зразку преамбул перед квитанцією не знайшлось узагалі. Гейт тиші
  60→40 мс (стокова відповідала на ~80 мс). Додано **ревізію формату** в діагностику
  (`SMS_ACK_FORMAT_REV`, видно в `sms_diag.py`) — щоб більше не гадати, яка прошивка залита.
  **Польова перевірка 4 — запис з ефіру BBD_0006 (стокова→форк, rev 5), декодовано.**
  Наша квитанція в ефірі **байт-у-байт правильна**:
  `01 40 26ea3d 26ea29 00 08 c241` (3 повтори о 2.353/2.412/2.472 с, CRC валідний,
  адреси правильні). Форма НЕ винна. Винен **таймінг**:
  - стокова передавала 0.674–1.992 с, наша квитанція почалась о **2.357 с** →
    **пауза 366 мс**, тоді як стокова сама квитує через **~80 мс** (BBD_0005). Вікно закрите.
  - Звідки 366 мс: після постановки квитанції той самий тік робив `store_add()` —
    **блокуючий erase+write SPI-флеша на сотні мс**, який тримав головний цикл; плюс 40 мс
    гейта тиші й 100 мс відкладеного ключування в `dmrDataTxLoad()`.
  Ітерація 6: квитанція будується й віддається в ефір **негайно** (до розшифровки й до
  будь-якого запису у флеш); запис вхідного у теку відкладено до кінця передачі (той самий
  прийом, що для «Надісланих»); додано `dmrDataTxLoadDelayed()` і ключування через **15 мс**
  замість 100. Очікувана пауза — десятки мс замість 366.
  **Польова перевірка 5 (rev 6): стокова все одно повторює.** Діагноз: перестрибнули в
  інший бік. PDU збирається на ОСТАННЬОМУ блоці даних, після якого відправник ще шле
  термінатор — тобто rev 6 лізла в ефір, поки стокова передає (і тому глуха). Разом два
  заміри задали вікно з обох боків: rev 5 = 366 мс (пізно), rev 6 ≈ 0 мс (рано),
  еталон стокової ~80 мс після КІНЦЯ передачі.
  Ітерація 7 — поєднання обох виправлень: чекаємо тиші в каналі **30 мс**, ключуємо через
  **15 мс** (разом ~45–60 мс після кінця), а блокуючий запис вхідного у флеш відкладено
  доти, доки квитанція не пішла (`s_pendInbox` + гейт `!s_ackPending`) — саме він з'їдав
  вікно в rev 5. Додано **самовимір паузи** (`s_ackLastDelayMs`, видно в `sms_diag.py`),
  тож таймінг тепер перевіряється однією командою, без HackRF.
  **Польова перевірка 6 (rev 7): пауза 30–46 мс (у вікні), форма доведено правильна —
  стокова ВСЕ ОДНО пише помилку.** Тобто перебрано й відкинуто геть усе: детекція,
  фільтр, код відповіді, подача, таймінг, байти. Стокова нас фізично чує (наші звичайні
  SMS вона приймає тим самим шляхом). Довідка: RT4D стокової взагалі не приймає, тож
  еталона «квитанція, яку стокова ЗАРАХУВАЛА» здобути ні з ким — у BBD_0005 її власне
  повідомлення на RT4D теж лишилось без відповіді.
  Лишилась єдина відмінність від стокової, яку ми самі й створили: вона шле квитанцію
  **одним** burst-ом через **~80 мс**, ми — **трьома** через 30 мс. Три Response header
  підряд вона могла зарахувати як дублікати, а 30 мс — влучити в її перехід
  передача→прийом.
  Ітерація 8: типово шлемо рівно як стокова (1 burst / 80 мс), і — головне — **обидва
  параметри тепер підбірні по USB** (`dmrSmsAckSetTuning`, команда 0xB2,
  `sms_diag.py --ack <повторів> <пауза_мс>`), щоб перебирати варіанти за хвилини, а не
  прошивками по одній. Підбір діє до вимкнення рації.
  **Польова перевірка 7: жодне значення сітки (1/60…150, 2/100) не допомогло** — отже
  подача (повтори/пауза) ні до чого.
  Ітерація 9 — **перевертаємо еталон**. Дотепер я копіював те, що стокова НАДСИЛАЄ (її
  квитанція йде голим заголовком без преамбул). Але правильний еталон — те, що вона
  успішно **ПРИЙМАЄ**, і такий приклад у нас власний: наше звичайне SMS стокова приймає,
  а воно йде з **6 CSBK-преамбулами** (`dmrSmsSend`). Схоже, її приймач не «заводить»
  data-виклик без преамбули; RT4D поблажливіший, тому безпреамбульна квитанція стокової
  йому зайшла. Коли преамбули були в нас (rev ≤4), тоді стояв хибний код `00 04` —
  тобто поєднання «преамбули + правильний код `00 08`» не перевірялось ЖОДНОГО разу.
  Тепер типово 6 преамбул, і їх кількість теж підбірна:
  `sms_diag.py --ack <повторів> <пауза_мс> [преамбул]`.
  **Польова перевірка 8: жоден варіант із преамбулами теж не підійшов.**

  **ВИСНОВОК ПО ГІЛЦІ «щоб СТОКОВА зарахувала нашу квитанцію» — ЗУПИНЕНО (2026-09-12).**
  Перебрано й доведено справним геть усе, що від нас залежить: детекція CONFIRMED-заголовка,
  фільтр «нам/приватне», код відповіді, форма байт-у-байт (звірено з ефірним записом),
  таймінг (сітка 30…150 мс, самовимір у прошивці), подача (1–3 повтори, 0/6/16 преамбул).
  Стокова нас фізично чує — наші звичайні SMS вона приймає тим самим шляхом.
  Ключове, чого бракує й узяти ніде: **жодного прикладу квитанції, яку ЦЯ стокова
  зарахувала б від будь-кого**. У BBD_0005 її власне повідомлення на RT4D лишилось без
  відповіді, а RT4D її повідомлень взагалі не приймає. Тобто цілком імовірно, що
  підтверджена доставка в цій прошивці в прямому режимі просто не працює (розрахована на
  ретранслятор/ARS), і жодне збігання байтів цього не виправить.
  Що лишається корисним: наша квитанція **коректна за стандартом і за форматом самої
  стокової**, перевірена в ефірі й хостовим тестом — вона працюватиме з рацією, яка її
  зараховує. Підбір по USB (`--ack`) лишається в коді як інструмент на майбутнє.
  Застереження (напрямок TX): власні повідомлення форк усе ще шле **Unconfirmed** (не просить
  квитанції), тож на форку-ВІДПРАВНИКУ поки нема індикації «доставлено». Це окремий, більший
  шматок (треба rate-3/4 кодер + читання вхідної `01 40`) — план нижче, п.11.

**RCTL — керування у стоковому форматі TYT (сумісність із заводською рацією):**
- Реверс формату команд байт-у-байт, CRC перевірено, хостові тести. Підтверджено й третьою
  рацією (RT4D шле той самий формат).
- TX: форк шле всі 4 команди (Radio Check / Monitor / Disable / Enable) у стоковому форматі.
  Monitor підтверджено в ефірі (стокова реагує).
- RX (форк як ціль): приймає стокові команди, гейт дозволів (перевірено: Check і Disable/Enable
  на залізі).
- **Квитанція Radio Check («онлайн»), обидва напрямки — перевірено на залізі 2026-09-11.** ✅
  Формат знято з ефіру HackRF-ом і декодовано власним декодером: `a4 10 00 80 <хто питав>
  <хто відповідає> <CRC>` — тобто отримана команда зі старшим бітом керуючого байта.
  Форк як ЦІЛЬ відповідає (стокова/RT4D показують його онлайн); форк як КОМАНДИР показує
  велику зелену галочку або червоний хрест. Ключове для роботи: для команд RCTL передача
  обривається ОДРАЗУ після останнього бургста (без термінатора й TX_END) — інакше власний
  хвіст передачі глушить квитанцію, яка починається вже через 30 мс (`RCTL_COMPAT.md` §5h).
- **Усі 4 команди RCTL працюють В ОБИДВА БОКИ, перевірено на залізі (2026-09-12):** форк
  як командир і як ціль для Radio Check / Monitor / Enable / Disable, з правильними
  квитанціями (Check/Enable/Disable — правило старшого біта; Monitor — власний формат
  `a0 10 9d 00`). Monitor-ціль відкриває прихований мік. Деталі — `RCTL_COMPAT.md` §5–§8e.
- **Квитанції Enable/Disable — знято з ефіру, реалізовано, хостові тести (2026-09-12).**
  Те саме правило (старший біт керуючого байта): Enable `a4 10 00 fe …`, Disable
  `a4 10 00 ff …`. Командир чекає підтвердження й на Enable, і на Disable — та сама
  галочка/хрест, звіряється команда квитанції. Формат узагальнено на всі команди
  (`dmr_rctl_stock_*_for()`); Monitor квитанції не має. Деталі — `RCTL_COMPAT.md` §6.
  **Лишилось перевірити на залізі:** галочка на Enable/Disable проти стокової.
- **Disable/Enable → справжній сон (stun):** заблокована рація виглядає вимкненою (чорний
  екран, тиша, клавіші й регулятор гучності не реагують), але приймач і обробка RCTL живі —
  Enable по ефіру її будить. Стан переживає перезавантаження (флеш). APO в сні не вимикає
  рацію. Аварійний вихід кабелем: `rctl_capture.py --unlock`. Усі сценарії перевірені.
  **NB (2026-09-12):** заблокований форк на Radio Check ВІДПОВІДАЄ квитанцією — як і стокова
  (перевірено: стокова у стуні теж тікає). Тобто галочка на Check заблокованої рації = «в зоні,
  розбудиться», а не «працює». Гейт дозволів діє й у сні; локальний «мертвий» вигляд лишається.
- Дозволи RCTL: пер-командні (Motorola/Hytera-модель). Відновлення після прошивки з ПК:
  `rctl_capture.py --rctl-on|--rctl-off|--rctl-status` (дозволи злітають при прошивці —
  це процес прошивки, не код; код збереження коректний, переживає вимк/увімк).
- Меню «Доступ RCTL»: 4 пункти (Перевірка/Моніторинг/Вимкнення/Ввімкнення), без зайвого
  майстер-перемикача; підказка під валкодер (↑/↓ рух, валкодер — On/Off).

**Інше:** попап «поза зоною ретранслятора» (правильний напис і час); S-метр; іконка та
екран вітання; захист anti-replay переживає вимкнення; інструмент захоплення сирих burst
(`rctl_capture.py --arm`, ловить будь-який data-sync burst, у т.ч. CRC-невалідний).

**Партія правок з фідбеку fleet (2026-09-11):**
- **#1 Фікс. PTT (залипання).** Стокова логіка вмикала залипання лише за заданого TOT на
  каналі, тож на каналах без TOT воно не працювало. Тепер вмикається завжди; для каналів
  без TOT — запобіжний таймаут 120 с (авто-зняття) з коротким сигналом. Перевірено на залізі. ✅
- **#2 Порожні зони.** Перемикання зон валкодером І список у меню «Зони» пропускають порожні
  (без каналів); «Всі канали» завжди видно. ✅
- **#4 GPS з меню.** Пункт GPS прибрано і з головного меню, і з «Загальних опцій» (рація без
  GPS-приймача); HAS_GPS/APRS лишились через `FORK_NO_GPS_MENU`. ✅
- **#5 Вікно гучності/шумоподавлення.** Повноекранне: заголовок, велике число у відсотках,
  смуга. Кольори за темою: НІЧ -- яскраво-жовтий на темному; ДЕНЬ -- темно-синій на білому. ✅
- **#6 Годинник + заряд разом.** Раніше при увімкненому годиннику заряд не показувався взагалі
  (ділили один правий кут). Тепер годинник праворуч, заряд ліворуч від нього. Перевірено. ✅
- **#7 APRS-прийом після бікону.** DMR RX-стан скидається після кожного бікону навіть без QSY
  (зайнятий канал + PTT більше не «вішає» прийом). Потребує польової перевірки.
- **#8 Ключі AES.** Прибрано Global TX key; шифрування береться лише з каналу; стандарт=вимкнено;
  порядок Off, Key 1…; прибрано inherit. ✅
- **#9 Літера «ю».** Гліф [254] font_8x16 перемальовано (стійка + перемичка + чисте кільце),
  без злипання з сусідньою «ч». Перевірено на залізі. ✅
- **Назва прошивки OpenGD77 → OpenTYT** (екран «Прошивка»/firmware info та голосова підказка).
- Верстку #5/#6/#9 звірено піксельним рендером (`tools/render_screen.py`-стиль: реальні шрифти
  UA-набору + реальні кольори тем).

## НЕ зроблено / відкладено

1. ~~**ACK (квитанція «онлайн» у командира)**~~ — **ЗРОБЛЕНО, перевірено на залізі
   (2026-09-11).** Обидва напрямки працюють: стокова бачить наш форк онлайн, і форк показує
   зелену галочку на стокову. Деталі формату й реверсу — `RCTL_COMPAT.md` §5a–§5h.

2. ~~**Monitor як ціль (прихований мікрофон)**~~ — **ЗРОБЛЕНО, перевірено на залізі**
   (2026-09-12). На дозволений Monitor форк шле квитанцію `a0 10 9d 00 <командир> <ціль>`
   (власний формат монітора, знято з ефіру — НЕ правило біта; §8e) і відкриває мік:
   приватний голосовий виклик тому, хто запитав, повний час. Приховано (без LED/екрана/
   бліпів), шифр ключем каналу, тривалість на рації (меню «Доступ RCTL» → «Час моніт.»,
   10–120 с). Гейт ALLOW_MONITOR; не перебиває чужу передачу; PTT оператора обриває. Межа:
   лише власний флот. Деталі — `RCTL_COMPAT.md` §7–§8e.
3. ~~**Radio Check як ціль**~~ — **ЗРОБЛЕНО** (п.1). Форк відповідає на дозволений Check
   стоковою квитанцією; заблокований (stun) форк на Check теж відповідає — як заводська
   (§6a: стокова у стуні не мовчить). Локальний «мертвий» вигляд рації лишається.
4. **Дрібне на перевірку:** витіснення кеша anti-replay при 8+ різних командирах; побайтна
   звірка запису AES-ключів з ПК; `gui_flasher` — не валідований наскрізно.
5. **#11 Формати SMS + повна квитанція (ACK).**
   - **Формат сумісності = «M» (Motorola).** Наш формат = IPv4/UDP/TMS, SAP=4 (IP based packet
     data) — підтверджено декодом ефіру (BBD_0005). У CPS стокової TYT це пункт **`M`** (не
     `DMR`, що є ETSI UDT — інша схема, форк її не розуміє). На RT4D — `Motorola`. Тобто для
     взаємодії форк ↔ стокова ↔ RT4D усі три ставлять Motorola/M-формат, однакові
     контакт/TG/CC/слот.
   - **Квитанція, напрямок RX (хтось → форк): ЗРОБЛЕНО** (див. «Зроблено», розділ SMS). Форк
     тепер квитує вхідне CONFIRMED SMS `01 40 …` — відправник бачить «доставлено».
   - **Квитанція, напрямок TX (форк → хтось): ЛИШИЛОСЬ.** Щоб форк-ВІДПРАВНИК бачив
     «доставлено», треба: (а) слати власні повідомлення як **CONFIRMED** (DPF=3, біт A=1) з
     блоками **rate-3/4** (18-байтний блок = DBSN+CRC9 + 16 навантаження) — потрібен rate-3/4
     кодер (наразі є лише декодер) + треліс-кодування; (б) приймати вхідну `01 40` на нашу
     адресу й ставити «доставлено» біля відправленого. Це більший шматок; квитанцію-читач
     (б) можна додати дешево, але без CONFIRMED-TX (а) її нічим викликати.
   - Hytera-формат — окремо, за потреби; знімати з ефіру HackRF-ом так само.
   - Інструмент реверсу: `tools/dmr_data_rx.py` (C16 → data-burst-и BPTC(196,96) + CRC-маски
     0x5A5A CSBK / 0x3333 data-header). Ним і декодовано BBD_0005.

## Робочий процес (нагадування)

**Середовище користувача: Termux на Android.** Репозиторій — `~/OpenGD77-AES256`
(НЕ `~/OpenGD77-AES256-fork`). Завантажені файли лягають у `/sdcard/Download/`
(Android, «Download» без «s»); теки `~/Downloads` НЕ ІСНУЄ — не пропонувати її.
Пуш у GitHub робить користувач з телефона (свій SSH-ключ); з хмарного середовища
github.com заблоковано проксі — обхід тільки через `git bundle`.

Готовий блок для застосування бандла (сам знаходить найсвіжіший, шлях вводити не треба):

```
cd ~/OpenGD77-AES256 && \
B=$(ls -t /sdcard/Download/*.bundle 2>/dev/null | head -1) && \
echo "Бандл: $B" && git pull "$B" main && git push
```

- Прошивка форка: блок вище → зелений CI → прошити `.sgl` →
  залити AES-ключі з ПК → `rctl_capture.py --rctl-on` (відновити дозволи RCTL).
- Тести RCTL: `rctl_capture.py --rxdiag` (лічильники прийому), `--rctl-status` (дозволи),
  `--unlock` (кабельне пробудження зі сну).
