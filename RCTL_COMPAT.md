# Сумісність RCTL зі стоковим протоколом TYT — план реверс-інжинірингу

**Рішення (2026-09-05, за вказівкою користувача):** «нам потрібна максимальна сумісність
з існуючими протоколами, а не щось окреме». Отже наш власний 16-байтний PDU
(`dmr_rctl_pdu.h`) — **тимчасовий**. Мета — щоб радіоперевірка (і згодом stun/revive)
йшли в ефір **у тому самому форматі, що й стокова TYT**, і щоб стокова рація відповідала
нашій, а наша — стоковій. Рівно так, як уже зроблено зі SMS.

Ціль сумісності — **стокова TYT MD-UV3x0** (Motorola/Hytera користувач не обрав; їх тут
свідомо немає — див. «Межі» нижче).

---

## 0. Вузол, який вирішує все: а чи стокова TYT взагалі це вміє? ✅ ЗАКРИТО

**Відповідь (2026-09-05, фото меню стокової TYT):** так, стокова має **всі чотири**
команди — `Radio Check`, `Remote Mon.`, `Radio Enable`, `Radio Disable`. Це стандартний
MOTOTRBO-подібний набір. Отже є реальний формат в ефірі, який можна захопити й відтворити,
і стокова рація в руках для звірки. Реверс розблоковано — далі розділ 2.

Нижче — оригінальне міркування, лишаю для контексту.

Перш ніж писати бодай рядок коду, треба підтвердити **факт**, а не припущення:

> **Чи є в CPS/меню стокової TYT MD-UV3x0 функція «Radio Check» (і «Radio
> Disable/Stun», «Radio Enable», «Remote Monitor»)?**

Це не риторичне питання. Стокова прошивка TYT доволі проста, і я **не можу стверджувати
з пам'яті**, що радіоперевірка там є. SMS там точно є (ми його й реверснули), а от
командні функції — під питанням.

Три можливі результати:

| Що виявиться | Наслідок |
|---|---|
| Стокова TYT **вміє** Radio Check | ✅ Є що захопити й відтворити. Ідемо розділом 2. |
| Стокова TYT **не вміє** нічого з цього | Немає «існуючого протоколу TYT» для цих команд. Тоді єдиний «існуючий протокол» — стандарт ETSI/DMRA (окрема робота, стокова рація не потрібна). |
| Вміє частину (напр. Radio Check є, Stun немає) | Робимо сумісним те, що є; решту — за стандартом. |

**Дію не можна пропустити.** Якщо збудувати «сумісний» формат наосліп і він не збіжиться
зі стоковим побайтово — стокова рація його просто проігнорує, і ми отримаємо той самий
«Немає відповіді», але вже з ілюзією, що зробили сумісність.

---

## 1. Безкоштовна перша перевірка (робиться вже зараз, без нової прошивки)

У нашій прошивці вже є USB-діагностика, яка рахує **кожен** прийнятий data-burst будь-якого
типу (`dmrSmsRxDiagBurst`, HR-C6000.c). Інструмент — `tools/sms_diag.py`.

1. Одна рація — **наша** (фork), під'єднана USB до ПК, на спільному з тестом каналі.
2. Друга — **стокова TYT**.
3. На ПК:  `python3 tools/sms_diag.py --reset`   (обнулити лічильники)
4. На стоковій TYT — **зробити радіоперевірку** нашої рації (якщо така функція є в меню).
5. На ПК:  `python3 tools/sms_diag.py`   (прочитати лічильники)

Як читати результат (`d`=усі data-burst, `hOk/hBad`=заголовки типу 6, `bOk/bBad`=rate-1/2
типу 7):

| Що показали лічильники | Висновок |
|---|---|
| `d` **не змінився** | Стокова нічого не надіслала в ефір як дані. Або функції немає, або вона йде голосовим/іншим класом. |
| `d` зріс, а `hOk`/`bOk` **ні** | Прийшов burst **не типу 6 і не типу 7** — майже напевно **CSBK (тип 3)**. Саме так найчастіше й переносять radio-check. Це — сигнал, що треба розділ 2. |
| зросли й `hOk`/`bOk` | Стокова шле це як звичайний data-PDU (тип 6+7), як SMS. Тоді захоплення в нас уже майже готове (див. пастку нижче). |

Ця перевірка коштує нуль коду й одразу каже, з чим ми маємо справу.

### ⚠️ Пастка: CSBK ми зараз НЕ дампимо

У HR-C6000.c дамп сирих байтів читається (сторінка 0x02) **лише для типів 6 і 7**:

    if (crcOk && ((rxDataType == 6) || (rxDataType == 7)))
    {
        ... dmrSmsRxBurst / dmrRctlRxBurst ...
    }

CSBK — це **тип 3**. Його `dmrSmsRxDiagBurst` **порахує** (тому `d` зросте), але самі
байти нікуди не потраплять. Тобто якщо стокова шле radio-check як CSBK — а це найімовірніше —
наш поточний захват побачить «щось прийшло», але не покаже, що саме. Це й треба
дописати в кроці 2.1.

---

## 2. План реверсу (той самий метод, що спрацював зі SMS)

Метод SMS задокументований у шапці `tools/dmr_enc_sms.py`: *«Framing CRCs reproduced
byte-exact from off-air captures»* — тобто захопили реальний ефір, розібрали, відтворили
байт-у-байт, звірили на залізі. Повторюємо крок у крок.

### 2.1. Захоплення сирих байтів команди ✅ ЗРОБЛЕНО (2026-09-05)

Готово й перевірено:
* `dmr_rctl_cap.c/.h` — кільце на 8 сирих burst у CCM (тип + 12 інфо-байтів), armed лише
  з ПК, "стоп-коли-повне" береже початок послідовності. Хостовий тест `test_dmr_rctl_cap.c`
  (7/7 зелено).
* хук у `HR-C6000.c`: коли захоплення ввімкнено, сторінка 0x02 читається для **будь-якого**
  типу data-sync, зокрема **CSBK (тип 3)**, а не лише 6/7. У звичайній роботі вимкнено —
  зайвого SPI-читання в ISR немає.
* USB-команди `0x98` (arm/disarm), `0x99` (dump), `0x9A` (reset) у `usb_com.c`.
* інструмент `tools/rctl_capture.py`.

**Сеанс захоплення (на робочому місці, дві рації):**

1. Форк-рація (RX, по USB до ПК) і стокова TYT на **спільному** каналі (частота/TS/CC/TG),
   мінімальна потужність, канал тихий.
2. `python3 tools/rctl_capture.py --arm`
3. На **стоковій** зробити **одну** дію — напр. `Radio Check` на DMR ID форк-рації.
4. `python3 tools/rctl_capture.py` — надрукує захоплені burst у hex.
5. Повторити для `Remote Mon.`, `Radio Enable`, `Radio Disable` (щоразу `--arm` перед дією).
6. Надіслати роздруківки — за ними робимо розбір (2.2).

Якщо нічого не захопилось — спершу `sms_diag.py` (розділ 1): чи `d` узагалі росте.

### 2.2. Розбір формату ✅ ЗРОБЛЕНО (2026-09-05)

Захоплено всі чотири команди зі стокової TYT (tools/rctl_capture.py), CRC перевірено
математично на кожному кадрі. **Формат — стандартний Motorola CSBK (тип 3), ВІДКРИТИМ
ТЕКСТОМ (жодного AES).**

Кадр команди — 12 байт:

    [b0] [FID=0x10] [0x00] [cmd] [src:3] [dst:3] [crc:2]

| Команда | b0 | опкод CSBK | cmd (b3) |
|---|---|---|---|
| Radio Check    | 0xA4 | 0x24 | 0x00 |
| Remote Monitor | 0x9D | 0x1D | 0x01 |
| Radio Enable   | 0xA4 | 0x24 | 0x7E |
| Radio Disable  | 0xA4 | 0x24 | 0x7F |

* Check / Enable / Disable — той самий опкод 0x24, різняться керуючим байтом (Enable/Disable
  = 0x7E/0x7F, різниця в один біт, як Inhibit/Uninhibit у Motorola). Monitor — окремий
  опкод 0x1D.
* FID = 0x10 (Motorola/DMRA) на всіх.
* Порядок адрес у команді — **src, потім dst** (у преамбулі навпаки: dst, src).
* CRC — CRC-CCITT "d" (poly 0x1021, init 0, xor 0xFFFF) над 10 байтами ^ 0xA5A5. Той самий,
  що ми вже маємо для преамбул SMS.

Перед командою — послідовність преамбул CSBK, що рахують кадри вниз від ~16:

    [0xBD] [0x00] [0x00] [лічильник] [dst:3] [src:3] [crc:2]

> ℹ️ **Про ID у захватах.** Ціль `2550287` (0x26EA0F) — це ID, який набрали на стоковій під
> час зняття; **справжній ID форк-рації — 2550313** (0x26EA69). На формат це не впливає
> (змінюються лише 3 байти адреси + CRC, які білдер рахує для будь-якого ID), тож захвати
> лишаються валідним провенансом. Для двонапрямної валідації (крок 2.5) цілитись треба на
> 2550313.

**Сирі захвати (стокова 2550333 -> набрана ціль 2550287), для провенансу:**

    check:   a4 10 00 00 26 ea 3d 26 ea 0f 93 7c
    monitor: 9d 10 00 01 26 ea 3d 26 ea 0f a3 88
    enable:  a4 10 00 7e 26 ea 3d 26 ea 0f 25 95
    disable: a4 10 00 7f 26 ea 3d 26 ea 0f 9d f4

### 2.3. Відтворення TX ✅ РЕФЕРЕНС ГОТОВИЙ (2026-09-05)

`tools/rctl_stock.py` будує всі чотири команди й обидва типи преамбул. Self-test у ньому
відтворює реальні захвати **байт-у-байт** (усі 4 команди + 3 преамбули). Це python-референс;
наступний крок — портувати цей формат у прошивку (2.4).

### 2.4. Заміна, а не паралель

Прибрати `dmr_rctl_pdu.h`-формат з ефіру й пустити стоковий. **Модель дозволів
(`Доступ/Перевірка/Моніторинг/Вимкнення/Ввімкнення`) лишається** — вона керує тим, що
рація погоджується **виконати**, і не залежить від формату на дроті. Anti-replay теж
лишається там, де формат це дозволяє.

### 2.5. Валідація на залізі

**✅ ПІДТВЕРДЖЕНО В ЕФІРІ (2026-09-05): форк -> стокова, команда Monitor.**
Форк надіслав Remote Monitor на стокову (2550333) стоковим форматом. Стокова **прийняла
команду й сама вийшла в ефір** приватним голосовим викликом на форк -- захоплено Voice-LC
header:

    03 00 00 26 ea 29 26 ea 3d ...   (FLCO 0x03 приватний; dst=2550313 форк; src=2550333 стокова)

Це доводить, що наш TX байт-правильний і заводська рація виконує наші команди. Radio Check /
Enable / Disable ідуть тим самим TX-шляхом.

**Ще не зроблено:**
* ACK Radio Check від стокової форк не встиг зловити (таймінг: миттєвий CSBK одразу після
  нашого TX, поки форк ще в teardown -- на відміну від Monitor, де голос триває секунди).
  Потрібен для екрана "рація на зв'язку" і для відповіді форку як цілі.
* RX-бік: форк як ЦІЛЬ (розпізнати вхідну стокову команду -> виконати: Check=ACK,
  Monitor=відкрити мік, Enable/Disable=локнутись/розлочитись), під дозволами.

---

## 3. Межі (свідомо)

- **Motorola / Hytera:** користувач їх не обрав, і реального обладнання для звірки немає.
  Будувати «наосліп» за напівзгаданими опкодами я не буду — це рівно той клас
  припущень, який уже двічі коштував цьому проєкту дефектів (частота тіків, UTF-8).
- **Універсальний stun/monitor чужих комерційних рацій** лишається за межею, яку ми
  провели в PLANS.md (розділ 3): сумісність «тільки власний флот» — так, інструмент проти
  третіх осіб — ні. Стокова TYT у власному флоті — це саме дозволений випадок.
- Поки не пройдено розділ 0, оцінки обсягу немає: усе залежить від того, що реально
  вміє стокова рація.

---

## 4. Що зробити першим (порядок)

1. ~~Перевірити наявність команд на стоковій.~~ ✅ є всі чотири (фото).
2. ~~Дописати захоплення сирих байтів.~~ ✅ зроблено (крок 2.1).
3. ~~Захопити 4 команди зі стокової.~~ ✅ зроблено (2026-09-05).
4. ~~Розбір формату (2.2) + референс TX (2.3).~~ ✅ зроблено, CRC і байти перевірено.
5. ~~Портувати стоковий формат у прошивку замість нашого PDU (2.4) — TX цих CSBK і RX.~~
   ✅ зроблено: TX усіх 4 команд, RX із гейтом дозволів, стокова Disable/Enable → сон/
   пробудження форка (повний stun: екран/звук/клавіші мертві, приймач живий), кабельний
   і ефірний вихід. Дозволи RCTL відновлюються з ПК (`rctl_capture.py --rctl-on`).
6. **Разом:** валідація стокова ↔ наша, обидва напрямки (2.5). ✅ Monitor і Disable/Enable
   підтверджено на залізі.

---

## 5. ВІДКЛАДЕНО: квитанція (ACK) — «онлайн/успішно» в командира

**Стан:** сон/пробудження/дозволи повністю робочі. Бракує лише щоб командир (стокова/
RT4D) бачив нашу рацію як «онлайн» після Radio Check і «успішно» після Disable/Enable —
для цього ціль має слати назад квитанцію-CSBK.

**Чому відкладено (2 бар'єри):**
1. *Формату не маємо.* Пасивно зловити квитанцію стокової з форка НЕ ВИЙШЛО, і це
   вичерпано: захоплювач уже ловить кожен data-sync burst із будь-якого слота й без
   перевірки CRC (HR-C6000.c, блок за `transmissionEnabled==false`), а відповідь у дампі
   не з'являється. Причина — на рівні радіочіпа: після завершення передачі командира чіп
   заново шукає синхронізацію й не встигає на єдиний короткий burst-квитанцію (без власної
   преамбули) у проміжку. Безпечної програмної зміни, що це полагодить, немає.
2. *Навіть з форматом — TX має бути слот-точним.* Квитанція мусить лягти у конкретний
   TDMA-слот одразу після команди; наш шлях «ISR→tick→dmrDataTxLoad» до цього не
   прив'язаний. Це окрема робота в TX-тракті.

**План, коли повернемось (є обладнання):** зняти формат квитанції з ефіру через
**HackRF + PortaPack** (ловить безперервно, без «сліпої зони» форка) або RTL-SDR +
DSD-FME/SDRTrunk (друкують CSBK у hex). Сценарій: RT4D/стокова робить Radio Check на
стокову (ціль), SDR ловить увесь обмін → маємо точні байти квитанції для Check; так само
для Disable/Enable. Далі — реалізувати слот-точний TX відповіді.

**Довідка (захоплено 2026-09-05):** RT4D (ID 2550299) → стокова (2550333), Radio Check:
преамбули `bd 00 00 NN 26ea3d 26ea1b crc` (відлік NN→1, dst,src), потім дві команди
`a4 10 00 00 …` (cmd=00) і `a4 10 00 80 …` (cmd=80), src=RT4D dst=стокова. Квитанції
стокової в захопленні НЕ було (див. вище).

---

## 5a. КВИТАНЦІЮ ЗНЯТО З ЕФІРУ (2026-09-11) -- бар'єр "формату не маємо" ЗНЯТО ✅

Формат квитанції Radio Check захоплено HackRF Portapack і декодовано власним
декодером (`tools/dmr_csbk_rx.py`), CRC валідний, відтворено у двох незалежних
записах.

**Квитанція стокової TYT (2550333) у відповідь на Radio Check від RT4D (2550299):**

    a4 10 00 80 26 ea 1b 26 ea 3d 3e 43
    |__| |  |__| |______| |______| |___|
     |   |   |       |        |      CRC
     |   |   |       |        +-- 2550333 = ХТО відповідає (стокова)
     |   |   |       +-- 2550299 = КОМУ (той, хто питав)
     |   |   +-- керуючі байти: 00 80  (у команді було 00 00)
     |   +-- FID 0x10 (Motorola/DMRA)
     +-- опкод 0xA4 (CSBKO 0x24 + LB) -- ТОЙ САМИЙ, що в команді

**Правило формування квитанції (просте):** взяти отриману команду, виставити
**старший біт керуючого байта** (0x00 -> 0x80), адресні поля лишити БЕЗ ЗМІН
(вони просто віддзеркалюються), перерахувати CRC. Стокова повторює квитанцію
~3 рази (хвіст в ефірі триває 82 мс ~= 3 burst-и).

**Як доведено, що це саме квитанція стокової, а не друга команда RT4D:**
1. *Вимкнення цілі.* Зі стоковою ВИМКНЕНОЮ RT4D повторює запит тричі (20 burst-ів),
   і 82-мс хвоста немає ЖОДНОГО. З увімкненою -- хвіст є завжди, рівно один,
   одразу після команди.
2. *Амплітуда.* Коли приймач стояв біля RT4D, хвіст був слабшим за команди
   (0.086 проти 0.100); коли біля стокової -- у 2.4 раза сильнішим (0.212 проти
   0.090). Співвідношення перевернулось разом із приймачем -> різні передавачі.
3. Байт 0x80 -- класичний прапорець відповіді (старший біт).

Раніше в цьому файлі значилось "дві команди a4 10 00 00 і a4 10 00 80, обидві
src=RT4D" -- **це було хибне припущення**: друга з них насправді і є квитанція
стокової. Адресні поля збігаються, бо квитанція їх віддзеркалює.

**Що лишилось для #10:** другий бар'єр -- слот-точний TX відповіді
(шлях ISR->tick->dmrDataTxLoad до TDMA-слота не прив'язаний). Формат більше не
блокує.

**Параметри декодування ефіру (здобуті емпірично, звірені по CRC) -- у
`tools/dmr_csbk_rx.py`:** мапінг ETSI (рівні -3,-1,+1,+3 -> дибіти 11,10,00,01);
burst = 98 info|10 slot-type|48 sync|10 slot-type|98 info; BPTC deinterleave
d[i]=raw[(i*181)%196], матриця 13x15 з індексу 1 рядково, інфо = рядки 0..8 x
стовпці 0..10 зі зсувом 3; CSBK CRC = crc16-CCITT(0x1021, init 0) ^ 0x5A5A, BE.

**Урок про захоплення ефіру:** КЛІП убиває дані остаточно, слабкий сигнал -- ні.
Перший придатний запис мав пік |IQ| 0.10 і декодувався; записи в насиченні
(пік 1.41) не врятувала навіть корекція помилок BPTC. Тримати пік 0.2..0.8;
якщо рація поруч -- зняти антену з Portapack і гейни в мінімум.

### 5b. Другий бар'єр був НЕ в слот-точності, а в повороті TX->RX (2026-09-11)

Коли форк сам перевіряє стокову, на екрані був хрест ("немає відповіді"). SDR-захват
показав, що стокова **відповідає справно**, а форк її просто не чує:

    наш форк (17 бургстів, слабший сигнал):
        bd 00 00 10 ... 01   преамбули 16->1   dst=2550333 src=2550313
        a4 10 00 00 | 26ea29 (ми) | 26ea3d (стокова)     <- команда, формат бездоганний
    стокова (сильніший бургст, 89.8 мс):
        a4 10 00 80 | 26ea29 (нам) | 26ea3d (від неї)    <- КВИТАНЦІЯ

**Вимірювання:** наша передача закінчується о 2.734 с, відповідь стокової починається
о 2.764 с -- **пауза лише ~30 мс**. (Для порівняння: стоковій на власну квитанцію
потрібно ~37 мс.) Тобто квитанція прилітає майже одразу за нашим хвостом.

**Причина проґавлення:** `dmrDataTxFinishPoll()` опитував завершення передачі раз на
**25 мс** і лише після цього кликав `trxDisableTransmission()` -> `trxActivateRx()`.
За 30 мс вікна ми в найгіршому випадку вмикали прийом, коли квитанція вже пролетіла.

**Виправлення (dmr_data.c):** період опитування 25 -> 5 мс (стеля таймауту виражена
через період, щоб не вкоротитись), і одразу після повернення в RX -- скид стану
DMR-приймача (`HRC6000ResetTimeSlotDetection()` + `HRC6000ClearActiveDMRID()`), та сама
пара, що лікувала невідновлення прийому після APRS-бікона (баг #7).

**Висновок для історії:** початкове припущення «квитанцію треба слати слот-точно» було
хибним у ОБИДВА боки. Відповідати -- звичайна коротка передача (командир чекає ~1.3 с).
А от ЧУТИ відповідь треба швидко: вікно ~30 мс, і воно впирається не в DMR, а в те, як
швидко прошивка повертається з передачі в прийом.

### §5c. Чому квитанції не було чути: апаратний CRC-гейт на CSBK (2026-09-11)

Після виправлення повороту TX→RX (§5b) на залізі все одно був хрест. Перегляд RX-шляху
дав другого підозрюваного, і він записаний у самому коді ще з часів реверсу:

```
// Під час реверсу ловимо ВСЕ, навіть CRC-невалідне: квитанція-відповідь цілі могла
// приходити з CRC, який HW відкидає, тож у звичайному (crcOk) захопленні її не видно.
```

Тобто ще тоді квитанція **не з'являлась у crcOk-захопленні** — її бачили лише тоді, коли
ловили все підряд. А робочий шлях прийому стокових команд був саме `crcOk && тип==3`.

Знято: CSBK (тип 3) тепер передається в `dmrRctlStockRxBurst()` **без апаратного crcOk**.
Це не послаблює перевірку — і `dmr_rctl_stock_parse()`, і `dmr_rctl_stock_parse_ack()`
рахують CRC16 програмно за тією самою формулою (`crc16d(...) ^ 0xA5A5`), тож шум відсівається
на рівень вище. Ціна — кілька зайвих SPI-читань в ISR на бита́ті бургсти; це ніщо.

### §5d. Діагностика шляху квитанції

Щоб наступна перевірка на залізі не була черговим здогадом, `--rxdiag` тепер показує три
лічильники (форк як КОМАНДИР):

| Лічильник | 0 означає |
|---|---|
| CSBK-бургстів прийнято | ефір не чуємо взагалі — поворот TX→RX або канал |
| з них розібрано як квитанцію | чуємо CSBK, але це не квитанція (формат/бітові помилки) |
| з них адресовано нам | квитанція чужа — звірити DMR ID цієї рації |

Якщо всі три ненульові, а на екрані хрест — квитанція прийшла пізніше за вікно очікування
(4 с), і лікується вже вікном, а не прийомом.

### §5e. Вимір: чи рація глуха після власної передачі (2026-09-11)

Лічильники §5d дали чисту картину на залізі:

| Тест | впізнано команд | CSBK прийнято |
|---|---|---|
| стокова → форк (контроль) | 1 | 3 |
| форк → стокова (наш випадок) | 0 | **0** |

Тобто прийом сам по собі справний (контроль), але після ВЛАСНОЇ передачі форк не бачить
жодного CSBK — навіть не «бачить, але відсіює». Знімати CRC-гейт (§5c) було правильно, але
недостатньо: проблема глибша за фільтри.

Щоб не гадати далі, додано вимір у самому перериванні «прийнято дані», **до всіх фільтрів**
(CRC, колір-код, slotState) — вікно 3 с після того, як `dmrDataTxFinishPoll` зняв передачу:

* скільки переривань узагалі (`winAny`) — 0 означає, що чіп мовчить, тобто рація справді
  не в прийомі; будь-яке ненульове означає, що чуємо, і питання у фільтрі;
* скільки з них із data-синхронізацією (`winData`);
* затримка до першого data-бургста (`winFirstMs`) — пряме число замість припущень;
* його тип, стан CRC і чи була ще активна передача (`winFirstInfo`);
* скільки тривало саме завершення передачі (`txEndMs`).

Очікуване з ефіру: квитанція стартує через ~30 мс і триває ~90 мс. Якщо `winFirstMs`
виявиться більшим за ~120 мс — винен поворот TX→RX (і тоді лагодимо шлях TX_END у DMO,
а не формат).

### §5f. Що показав вимір, і чому наступний крок — контрольний сигнал (2026-09-11)

Перший вимір на залізі:

```
завершення передачі зайняло: 1140 мс
переривань усього: 0
перший data-бургст: НЕ БУЛО ВЗАГАЛІ
```

**1140 мс — це нормально, не баг.** Стокова команда = 16 преамбул + сам CSBK, тобто 17
бургстів; у DMO активний слот трапляється раз на 60 мс, отже 17 × 60 ≈ 1020 мс плюс ~100 мс
на TX_END. Число сходиться з ефіром (SDR бачив нашу передачу такої ж довжини), тож затримки
в поверненні з передачі немає — вона тривала рівно стільки, скільки мала.

**Нуль переривань — неоднозначний.** Він однаково означає і «рація глуха», і «в ефірі просто
нічого не було». Апаратне переривання «прийнято дані» виникає лише коли є DMR-сигнал, тож
тиша в ефірі дає такий самий нуль, як і зламаний прийом. Розрізнити можна тільки контрольним
сигналом у те саме вікно.

Тому додано:

* `intTotal` — **безумовний** лічильник усіх переривань «прийнято дані» від обнулення, поза
  будь-якими вікнами й фільтрами. Якщо після контрольної передачі він лишиться нулем, то
  зламаний сам вимір (або прийом мертвий узагалі), і це видно за одну команду;
* вікно спостереження розширено 3 с → 15 с, щоб контрольний сигнал устигали подати вручну.

Процедура: обнулити → з форка зробити перевірку → **поки йде очікування, натиснути PTT на
стоковій** → прочитати. `intTotal > 0` при `переривань усього = 0` означало б, що вікно
взагалі не зводиться; обидва ненульові — рація чує після власної передачі, і тоді питання
не в повороті TX→RX, а в тому, чи стокова взагалі відповідає нашому форку в цьому сеансі.

### §5g. Прийом після власної передачі живий; ефір у вікні був порожній (2026-09-11)

Вимір із контрольним PTT:

```
завершення передачі зайняло: 1140 мс
переривань усього: 53
з них data-синхронізація: 4
перший data-бургст через: 4272 мс, тип 1 (VLC-hdr), CRC ок, передача ще активна: ні
```

**Рація НЕ глуха.** 53 переривання й чистий Voice LC Header із валідним CRC після власної
передачі означають, що приймач після TX повертається у справний стан. Гіпотеза «не встигаємо
повернутись у прийом» (§5b) як єдина причина — відпадає.

**Але вимір усе ще не доводить головного.** Контрольний сигнал прийшов аж на 4272 мс (рука
людини), а квитанція очікується на ~30 мс. Про проміжок 0…4272 мс ми знаємо лише те, що
переривань не було — а це однаково сумісно з «глухі перші сотні мс» і з «в ефірі було тихо».

Тому потрібен сигнал, який гарантовано присутній у момент кінця нашої передачі. Спосіб:
**тримати PTT на стоковій натиснутим і в цей час зробити перевірку з форка.** Наша передача
переб'є голос в ефірі, але це не важливо — міряємо не якість, а момент, коли приймач знову
щось чує. Для цього додано `winFirstAnyMs` — затримка до ПЕРШОГО переривання будь-якого
класу (голос дає їх безперервно, на відміну від рідких data-бургстів).

Читати так: `winFirstAnyMs` ≈ одиниці-десятки мс — приймач готовий вчасно, і тоді причина
хреста в тому, що стокова не відповідає; сотні мс — винен поворот TX→RX, і лагодити треба
шлях TX_END у DMO.

*Окремо, і це була моя помилка:* інструмент читав `ser.read(64)`, а відповідь на 16
лічильників — 67 байтів. Останній лічильник тихо губився, і скрипт визначав версію прошивки
як стару. Тепер читається заголовок, а потім рівно стільки байтів, скільки оголошено.

### §5h. Причина знайдена: ми заглушуємо квитанцію власним хвостом передачі (2026-09-11)

Захват `BBD_0000.C16` (500 кГц, форк 2550313 перевіряє стокову 2550333) розібрано
по-бурстово, з розділенням передавачів за рівнем сигналу:

| час | тривалість | пік \|IQ\| | вміст |
|---|---|---|---|
| 1204…2194 мс | 17 × 29.6 мс, крок 60 мс | 0.033 | наш форк: 16 преамбул `bd00` (лічильник 0x10→0x01) + команда `a4 10 00 00 \| 26ea29 \| 26ea3d` |
| 2224.3…2312.9 мс | 88.6 мс **суцільно** | 0.093 | стокова: квитанція, 3 бургсти поспіль |

Декодовано з CRC: `a4 10 00 80 | 26ea29 | 26ea3d` на 2.280 с. **Стокова відповідає, і
адресує квитанцію саме нам.** Питання «чи є вона в ефірі» закрите остаточно.

**Чому ми її не чуємо.** Наш останній бургст закінчується на 2194 мс, квитанція починається
на 2224.3 мс — через 30 мс. А наш передавач у цей час ще працює: після спорожнення черги
прошивка шле термінатор, потім TX_END_1 шле ще один, потім TX_END_2 і ще 29 мс на останній
бургст — разом ~120 мс. Тобто ми:

1. **глушимо власним сигналом** перші два з трьох бургстів квитанції — і це видно в захваті:
   декодер узяв лише третій (2.280 с), а перші два, попри вдвічі сильніший сигнал стокової,
   не зійшлися по CRC;
2. **повертаємось у прийом** приблизно тоді, коли квитанція вже закінчилась.

Обидва раніші виміри тепер сходяться: 1140 мс передачі (17 бургстів × 60 мс + хвіст) і
92 мс до першого переривання після зняття передачі.

**Виправлення.** Для стокових RCTL-команд як командир передача обривається негайно після
останнього бургста: без термінатора й без довгого TX_END (`dmrDataTxLoadFast()` →
`dmrDataTxFastEnd()` у `DMR_STATE_TX_2`, далі `HRC6000InitDigitalDmrRx()` і одразу
`DMR_STATE_IDLE`). Термінатор тут нічого не вартий: стокова відповідає ще до нього, тобто
вважає команду завершеною по самому CSBK. Звичайний `dmrDataTxLoad()` (SMS та інше) не
змінено — там термінатор лишається.

Очікування: передача гасне на ~2194 мс, прийом живий із ~2200 мс, уся квитанція (2224…2313)
потрапляє у вікно, та ще й трьома спробами поспіль.

### §6. Квитанції Enable/Disable — те саме правило (2026-09-12)

Після того як передача перестала глушити відповідь (§5h), квитанцію вже чути вбудованим
кільцем захвату — HackRF більше не потрібен. Знято з ефіру (форк 2550313 керує стоковою
2550333), CRC валідний у кожному:

```
Enable  ->  a4 10 00 fe 26 ea 29 26 ea 3d 49 2b
Disable ->  a4 10 00 ff 26 ea 29 26 ea 3d f1 4a
```

Правило квитанції з §5a **узагальнюється на всі команди** без винятку: квитанція = та сама
команда зі встановленим старшим бітом керуючого байта, адреси й опкод незмінні.

| команда | керуючий байт | квитанція |
|---|---|---|
| Check   | 0x00 | 0x80 |
| Enable  | 0x7E | 0xFE |
| Disable | 0x7F | 0xFF |

Monitor (0x01) квитанції не має — стокова на нього не відповідає CSBK.

Реалізація узагальнена: `dmr_rctl_stock_ack_for()` / `_build_ack_tx_for()` / `_parse_ack_for()`
приймають команду; вузькі старі функції лишились як обгортки над Check для сумісності.
Командирський бік (`menuRCTLRemote.c`) тепер чекає квитанцію й на Enable, і на Disable, і
показує ту саму зелену галочку / червоний хрест; звіряється КОМАНДА квитанції, щоб чужа
Check-відповідь не закрила Enable-очікування. Байти обох квитанцій — у хостових тестах
(`test_dmr_rctl_stock.c`).

**Важливо про Disable:** підтвердження приходить у той короткий проміжок, поки стокова ще
відповідає, ПЕРЕД тим як заснути. Тобто галочка на Disable = «команду прийнято й виконано»,
а не «рація ще на зв'язку». Після цього стокова мовчить (сон), і повторний Check дасть хрест
— це нормально. Підняти її можна тільки Enable по ефіру.

### §6a. Стокова відповідає на Check і в стуні — форк приведено у відповідність

Польове спостереження (2026-09-12): галочка на Radio Check з'являється **завжди, коли ціль
отримує команду, навіть коли вона дистанційно заблокована (stun)**. Тобто заводська TYT у
сні НЕ мовчить на Check — вона відповідає тією самою квитанцією `a4 10 00 80 …`.

Це спростувало припущення, зашите у форк раніше («заблокована має виглядати мертвою, тож і
на Check мовчить — як стокова»): насправді стокова так не робить. Форк приведено у
відповідність — заблокований форк тепер теж відповідає на Check.

Чому це безпечно: гейт дозволів (`dmr_rctl_stock_should_act`) діє незалежно від стуну —
форк відповідає лише тому, хто має право Check, а заблокував його лише той, хто мав право
Disable. Отже реачність («у зоні, розбудиться») відкривається виключно авторизованому
командиру. Локальний «мертвий» вигляд (чорний екран, тиша, глухі клавіші) — окремий від
RF-відповіді стан і зберігається.

Наслідок для оператора: галочка на Check заблокованої рації означає «ціль у зоні й її можна
розбудити Enable», а не «ціль працює». Розрізнити ці два стани по ефіру неможливо — квитанція
байт-у-байт однакова (протокол TYT не несе прапорця стуну).

### §7. Monitor як ціль — прихований мікрофон (2026-09-12)

Найделікатніша команда, свідомо остання. Межа незмінна (§3, PLANS.md §3): лише власний
флот, лише під гейтом дозволів. Формат Monitor знято з ефіру раніше: команда
`9d 10 00 01 <src> <dst> <crc>`; ціль на неї НЕ шле CSBK-квитанцію, а виходить приватним
голосовим викликом назад до того, хто запитав (захват 2026-09-05: Voice-LC FLCO 0x03,
dst=запитувач). Тепер форк як ЦІЛЬ це виконує.

**Поведінка.** На дозволений Monitor (ALLOW_MONITOR) форк відкриває мікрофон і передає
приватний голосовий виклик тому, хто запитав, `dmrRctlMonitorSecs()` секунд, тоді сам
зупиняється. Шифрування — ключем каналу, як звичайний голос (командир із ключем чує; на
відкритому каналі — відкрито).

**Прихованість (рішення власника).** Без червоного світлодіода, без TX-екрана, без бліпів:
не йдемо через UI_TX_SCREEN і не світимо LED. Голосовий TX сам кодує мік через AMBE, коли
`trxTransmissionEnabled=1` (HRC6000 tick) — тобто ключування таке саме, як PTT, лише без
візуальних ознак. Локальний екран лишається таким, яким був (RX-подібний).

**Тривалість (рішення власника).** Виставляється на рації: меню «Доступ RCTL» → «Час моніт.»
(10/20/30/60/120 с). Зберігається у блоці RCTL (версія 4: колишній reserved-байт став
monitorSecs; версія 3 читається як «типова», 30 с). Максимум 120 с.

**Запобіжники.**
- Гейт дозволів (`dmr_rctl_stock_should_act`, ALLOW_MONITOR) — відповідаємо лише
  авторизованому командиру, як і решта RCTL.
- Не перебиваємо чужу передачу: якщо вже йде голос/дані/монітор або не цифровий режим —
  Monitor ігнорується (лічильник monSkipped).
- Аварійне зняття: будь-яке PTT власного оператора негайно обриває монітор
  (`dmrRctlMonitorCancel()` у applicationMain перед власним TX) — і як пріоритет його
  передачі, і як вихід, якщо монітор увімкнули помилково.
- Працює й коли рація заблокована (stun): це і є сценарій «оцінити захоплену/втрачену
  власну рацію».
- Діагностика: `--rxdiag` показує monStarted (скільки разів ключували мік) і monSkipped.

**Лишилось перевірити на залізі:** що стокова-командир чує аудіо з форка на Monitor, що
екран/світлодіод форка нічого не видають, і що PTT обриває передачу. Це найризикованіша
зміна (чіпає голосовий TX-шлях), тож тестувати обережно й на мінімальній потужності.

### §8. Стокова-командир чекає квитанцію на КОЖНУ команду (2026-09-12, залізо)

Польовий тест: форк як ЦІЛЬ виконує Monitor/Enable/Disable, але стокова-КОМАНДИР чекає
відповіді й через кілька секунд видає сигнал помилки. Причина: ми навчили форк слати
CSBK-квитанцію лише на Radio Check. Enable/Disable міняли стан, але нічого не відповідали;
стокова їх не «закривала».

Виправлено: форк-ціль тепер шле квитанцію на всі команди, за тим самим правилом (§6):

| команда | квитанція |
|---|---|
| Check   | `a4 10 00 80 …` |
| Enable  | `a4 10 00 fe …` |
| Disable | `a4 10 00 ff …` |
| Monitor | `9d 10 00 81 …` |

Enable/Disable/Check — байти підтверджені захватом. **Monitor (`0x01 -> 0x81`) —
екстраполяція того самого правила бітом**, ще не звірена з ефіром: попереднє припущення
«Monitor квитанції не має» походило із захвату, де був лише обмін Check. Якщо на залізі
Monitor усе одно даватиме помилку — знімаємо його квитанцію HackRF-ом і корегуємо.

Порядок для Disable: спершу квитанція, потім стун (дана-TX не гатиться стуном, як і Check
у сні). Для Monitor: спершу CSBK-квитанція, потім, коли черга TX звільнилась, ключується
прихований голос (monitorTick чекає dmrDataTxActive, дедлайн 3 с).

### §8a. Монітор обривався за ~1 с: колізія з тим-же TX-шляхом квитанції (2026-09-12)

Залізо: Enable/Disable-квитанції прийшли (помилки немає), Monitor-квитанція теж прийнята
(стокова не помиляється — правило `0x01->0x81` підтверджено де-факто), АЛЕ прихований голос
ішов лише ~1 с і гаснув.

Причина — колізія. Ми шлемо CSBK-квитанцію через дана-TX (`dmrDataTxLoad`), а потім ключуємо
голос. Гейт старту голосу перевіряв лише `dmrDataTxActive()` (прапорець s_dataTxActive), а
той гасне, коли черга бургстів спорожніла — ДО того, як завершиться термінатор/TX_END і
`dmrDataTxFinishPoll` зробить `trxDisableTransmission`. Тобто голос ключувався в момент, коли
передача квитанції ще фізично тривала: teardown квитанції обривав свіжий голос (~1 с і кінець).

Виправлено: голос ключується лише коли TX-шлях квитанції ПОВНІСТЮ відпустив — гейт тепер
додатково вимагає `!trxIsTransmitting` (а не лише `!dmrDataTxActive()`). Плюс діагностика:
`--rxdiag` показує «тривалість останнього монітора» (мс) — має бути ≈ виставлена тривалість;
~1000 мс означало б, що монітор усе ще обривається зовні.

### §8b. Монітор: голос не ключувався взагалі — детерміноване ключування (2026-09-12)

Діагностика `--rxdiag` дала однозначну картину: Monitor прийнято (`виконано Monitor: 1`),
CSBK-квитанція вийшла в ефір (вікно показало передачу 310 мс), але `monStarted = 0` і
`monSkipped = 0` — голос не ключувався й навіть не «кидався». Тобто гейт-полінг у monitorTick
(`monitorStart` повертав 0, бо `trxIsTransmitting`/`dmrDataTxActive` не скидались чисто після
квитанції) просто зависав, не доходячи ні до старту, ні до дедлайну.

Прибрано гонку прапорців. Голос тепер ключується ДЕТЕРМІНОВАНО:
- у гілці Monitor шлемо CSBK-квитанцію й плануємо `monitorKeyVoice` через таймер (+800 мс),
  а не полінгом;
- `monitorKeyVoice` (головний цикл) чекає, доки квитанція фізично зійде (bounded-перенос по
  200 мс, до 10 спроб), тоді робить `HRC6000ForceDMRIdleForTx()` — той самий чистий стан
  IDLE, з якого надійно стартує дана-TX (скидає slotState, trxIsTransmitting, isWaking) — і
  `trxSetTX()`. Далі hrc6000Tick сам заводить TX_START → soundReceiveData (мік) → голос.

Тобто ключування голосу тепер спирається на ту саму перевірену примітиву, що й дана-TX, а не
на припущення, що teardown квитанції встиг усе поскидати. `monStarted`/`monLastMs` у `--rxdiag`
покажуть, чи голос стартував і скільки тривав.
