# Статус проєкту (оновлено 2026-09-19)

Коротка «пам'ять» про пророблене й що лишилось. Деталі RCTL — у `RCTL_COMPAT.md`,
плани фаз — у `PLANS.md`, посібник — у `USAGE.md`.

## Зроблено й перевірено на залізі

**SMS (зашифрована/чиста), обидва напрямки.** Формат — реверснута, валідована в ефірі
стокова схема TYT: AES-256-ECB над IPv4/UDP/TMS, порт 4007. Два режими: з ключем шифрує,
без ключа — чистий текст (сумісно зі стоковою). Адресний фільтр вхідних + перемикач монітора.
- **TX (форк→стокова):** Unconfirmed rate-1/2; заводська рація розшифровує наш TX. ✅
- **RX (стокова→форк):** стокова шле **CONFIRMED rate-3/4** (18-байтний блок = 2 байти
  DBSN+CRC9 + 16 байтів навантаження); форк пропускає 2 службові байти/блок, збирає PDU,
  CRC32 сходиться, показує текст. Перевірено на залізі 2026-09-05 (pdu/msg росли). ✅
- **Квитанція на вхідне CONFIRMED SMS (link-layer ACK) — реалізовано, хостовий тест
  (2026-09-12).** Формат знято з ефіру HackRF-ом (BBD_0005: стокова 0x26EA3D квитує RT4D
  0x26EA1B) і декодовано власним декодером `tools/dmr_data_rx.py`. Коли форк отримав усі
  блоки адресованого йому приватного CONFIRMED повідомлення (біт A=1, CRC32 PDU зійшовся),
  він відповідає **Response data header (DPF=1)**:
  `01 40 <кому:початк.відправник> <від кого:ми> 00 <BF> <CRC16^0xCCCC>` — SAP=4 (IP),
  Class=00 (ACK). Квитуємо ДО розшифровки (це про доставку кадрів, не про читання), тільки
  приватні й адресовані нам; групові й «моніторні»/чужі — ні.
  **Польова перевірка 1 (2026-09-12): квитанції нема, стокова шле повідомлення 3 рази
  (форк отримує всі три).** Тобто прийом справний, а квитанція не долітає. Виправлено й
  озброєно лічильниками (ітерація 2):
  - Ключування більше не по фіксованій паузі від завершення PDU, а **по ТИШІ в каналі**:
    чекаємо ≥60 мс без жодного data-burst (`s_lastRxBurstMs` оновлюється в
    `dmrSmsRxDiagBurst`, який кличеться на кожен data-sync burst). Причина: `dmrDataTxLoad()`
    сам відкладає ключування ще на 100 мс, тож стара схема влучала у хвіст/термінатор
    відправника — а він у цей момент ще передає і нас не чує. Дедлайн 2.5 с, далі кидаємо.
  - На `slotState` навмисно НЕ чекаємо (він тримається в RX-стані до END_TICK_TIMEOUT);
    детерміноване ключування робить сам `dmrDataTxLoad()` через `HRC6000ForceDMRIdleForTx()`.
  - **Лічильники `dmrSmsAckDiag()`** у хвості USB 0x93 (старі парсери цілі), читаються
    `tools/sms_diag.py`: бачив / вчергу / вефір / кинуто + **перші два байти останнього
    вхідного data-заголовка** + груповий?/нам?. Скрипт друкує готовий діагноз: чи стокова
    взагалі просить квитанцію (DPF=3 + біт A), чи відсіяв фільтр, чи не звільнявся канал.
  **Польова перевірка 2 (2026-09-12): лічильники показали `бачив=3 вчергу=3 вефір=3
  кинуто=0`, заголовок стокової `43 4c` (DPF=3, A=1, SAP=4) — тобто квитанція ЙШЛА в ефір
  усі 3 рази, а стокова однаково ретрансмітила.** Отже справа була не в таймінгу й не в
  детекції, а у ФОРМІ. Знайдено (ітерація 3): я переплутав призначення двох останніх байтів
  Response-заголовка. Правильно (як в ефірі):
  **`o8 = Blocks-to-Follow = 0x00`, `o9 = Class/Type/Status = 0x08`** (Class=00 ACK,
  Type=001) — ОБИДВА константи. Реалізація ж трактувала їх навпаки й підставляла в `o9`
  лічильник прийнятих блоків (у полі — 4), тобто слала `00 04` = Status=4 замість ACK.
  Хостовий тест цього не спіймав, бо приймав `bf` параметром і викликався саме з `0x08` —
  збіг з ефіром був випадковий. Тепер параметра немає, байти прибиті константами + окрема
  регресійна перевірка `o8/o9 == 00 08`.
  **Польова перевірка 3 (2026-09-12): `бачив=6 вчергу=4 вефір=4 кинуто=0`, заголовок `43 40`.**
  Квитанція знову йшла в ефір. Ітерація 5 — приводимо ПОДАЧУ до перевіреного зразка:
  **3 повтори заголовка-квитанції, БЕЗ CSBK-преамбул** (рівно як `dmr_rctl_stock_build_ack_tx`,
  чия квитанція з цією ж стоковою працює на залізі; команди RCTL шлються з 16 преамбулами, а
  квитанції — ні). Раніше було 2 преамбули + заголовок, тобто сам заголовок виходив на ~120 мс
  пізніше, а в ефірному зразку преамбул перед квитанцією не знайшлось узагалі. Гейт тиші
  60→40 мс (стокова відповідала на ~80 мс). Додано **ревізію формату** в діагностику
  (`SMS_ACK_FORMAT_REV`, видно в `sms_diag.py`) — щоб більше не гадати, яка прошивка залита.
  **Польова перевірка 4 — запис з ефіру BBD_0006 (стокова→форк, rev 5), декодовано.**
  Наша квитанція в ефірі **байт-у-байт правильна**:
  `01 40 26ea3d 26ea29 00 08 c241` (3 повтори о 2.353/2.412/2.472 с, CRC валідний,
  адреси правильні). Форма НЕ винна. Винен **таймінг**:
  - стокова передавала 0.674–1.992 с, наша квитанція почалась о **2.357 с** →
    **пауза 366 мс**, тоді як стокова сама квитує через **~80 мс** (BBD_0005). Вікно закрите.
  - Звідки 366 мс: після постановки квитанції той самий тік робив `store_add()` —
    **блокуючий erase+write SPI-флеша на сотні мс**, який тримав головний цикл; плюс 40 мс
    гейта тиші й 100 мс відкладеного ключування в `dmrDataTxLoad()`.
  Ітерація 6: квитанція будується й віддається в ефір **негайно** (до розшифровки й до
  будь-якого запису у флеш); запис вхідного у теку відкладено до кінця передачі (той самий
  прийом, що для «Надісланих»); додано `dmrDataTxLoadDelayed()` і ключування через **15 мс**
  замість 100. Очікувана пауза — десятки мс замість 366.
  **Польова перевірка 5 (rev 6): стокова все одно повторює.** Діагноз: перестрибнули в
  інший бік. PDU збирається на ОСТАННЬОМУ блоці даних, після якого відправник ще шле
  термінатор — тобто rev 6 лізла в ефір, поки стокова передає (і тому глуха). Разом два
  заміри задали вікно з обох боків: rev 5 = 366 мс (пізно), rev 6 ≈ 0 мс (рано),
  еталон стокової ~80 мс після КІНЦЯ передачі.
  Ітерація 7 — поєднання обох виправлень: чекаємо тиші в каналі **30 мс**, ключуємо через
  **15 мс** (разом ~45–60 мс після кінця), а блокуючий запис вхідного у флеш відкладено
  доти, доки квитанція не пішла (`s_pendInbox` + гейт `!s_ackPending`) — саме він з'їдав
  вікно в rev 5. Додано **самовимір паузи** (`s_ackLastDelayMs`, видно в `sms_diag.py`),
  тож таймінг тепер перевіряється однією командою, без HackRF.
  **Польова перевірка 6 (rev 7): пауза 30–46 мс (у вікні), форма доведено правильна —
  стокова ВСЕ ОДНО пише помилку.** Тобто перебрано й відкинуто геть усе: детекція,
  фільтр, код відповіді, подача, таймінг, байти. Стокова нас фізично чує (наші звичайні
  SMS вона приймає тим самим шляхом). Довідка: RT4D стокової взагалі не приймає, тож
  еталона «квитанція, яку стокова ЗАРАХУВАЛА» здобути ні з ким — у BBD_0005 її власне
  повідомлення на RT4D теж лишилось без відповіді.
  Лишилась єдина відмінність від стокової, яку ми самі й створили: вона шле квитанцію
  **одним** burst-ом через **~80 мс**, ми — **трьома** через 30 мс. Три Response header
  підряд вона могла зарахувати як дублікати, а 30 мс — влучити в її перехід
  передача→прийом.
  Ітерація 8: типово шлемо рівно як стокова (1 burst / 80 мс), і — головне — **обидва
  параметри тепер підбірні по USB** (`dmrSmsAckSetTuning`, команда 0xB2,
  `sms_diag.py --ack <повторів> <пауза_мс>`), щоб перебирати варіанти за хвилини, а не
  прошивками по одній. Підбір діє до вимкнення рації.
  **Польова перевірка 7: жодне значення сітки (1/60…150, 2/100) не допомогло** — отже
  подача (повтори/пауза) ні до чого.
  Ітерація 9 — **перевертаємо еталон**. Дотепер я копіював те, що стокова НАДСИЛАЄ (її
  квитанція йде голим заголовком без преамбул). Але правильний еталон — те, що вона
  успішно **ПРИЙМАЄ**, і такий приклад у нас власний: наше звичайне SMS стокова приймає,
  а воно йде з **6 CSBK-преамбулами** (`dmrSmsSend`). Схоже, її приймач не «заводить»
  data-виклик без преамбули; RT4D поблажливіший, тому безпреамбульна квитанція стокової
  йому зайшла. Коли преамбули були в нас (rev ≤4), тоді стояв хибний код `00 04` —
  тобто поєднання «преамбули + правильний код `00 08`» не перевірялось ЖОДНОГО разу.
  Тепер типово 6 преамбул, і їх кількість теж підбірна:
  `sms_diag.py --ack <повторів> <пауза_мс> [преамбул]`.
  **Польова перевірка 8: жоден варіант із преамбулами теж не підійшов.**

  **ВИСНОВОК ПО ГІЛЦІ «щоб СТОКОВА зарахувала нашу квитанцію» — ЗУПИНЕНО (2026-09-12).**
  Перебрано й доведено справним геть усе, що від нас залежить: детекція CONFIRMED-заголовка,
  фільтр «нам/приватне», код відповіді, форма байт-у-байт (звірено з ефірним записом),
  таймінг (сітка 30…150 мс, самовимір у прошивці), подача (1–3 повтори, 0/6/16 преамбул).
  Стокова нас фізично чує — наші звичайні SMS вона приймає тим самим шляхом.
  Ключове, чого бракує й узяти ніде: **жодного прикладу квитанції, яку ЦЯ стокова
  зарахувала б від будь-кого**. У BBD_0005 її власне повідомлення на RT4D лишилось без
  відповіді, а RT4D її повідомлень взагалі не приймає. Тобто цілком імовірно, що
  підтверджена доставка в цій прошивці в прямому режимі просто не працює (розрахована на
  ретранслятор/ARS), і жодне збігання байтів цього не виправить.
  Що лишається корисним: наша квитанція **коректна за стандартом і за форматом самої
  стокової**, перевірена в ефірі й хостовим тестом — вона працюватиме з рацією, яка її
  зараховує. Підбір по USB (`--ack`) лишається в коді як інструмент на майбутнє.
  Застереження (напрямок TX): власні повідомлення форк усе ще шле **Unconfirmed** (не просить
  квитанції), тож на форку-ВІДПРАВНИКУ поки нема індикації «доставлено». Це окремий, більший
  шматок (треба rate-3/4 кодер + читання вхідної `01 40`) — план нижче, п.11.

**RCTL — керування у стоковому форматі TYT (сумісність із заводською рацією):**
- Реверс формату команд байт-у-байт, CRC перевірено, хостові тести. Підтверджено й третьою
  рацією (RT4D шле той самий формат).
- TX: форк шле всі 4 команди (Radio Check / Monitor / Disable / Enable) у стоковому форматі.
  Monitor підтверджено в ефірі (стокова реагує).
- RX (форк як ціль): приймає стокові команди, гейт дозволів (перевірено: Check і Disable/Enable
  на залізі).
- **Квитанція Radio Check («онлайн»), обидва напрямки — перевірено на залізі 2026-09-11.** ✅
  Формат знято з ефіру HackRF-ом і декодовано власним декодером: `a4 10 00 80 <хто питав>
  <хто відповідає> <CRC>` — тобто отримана команда зі старшим бітом керуючого байта.
  Форк як ЦІЛЬ відповідає (стокова/RT4D показують його онлайн); форк як КОМАНДИР показує
  велику зелену галочку або червоний хрест. Ключове для роботи: для команд RCTL передача
  обривається ОДРАЗУ після останнього бургста (без термінатора й TX_END) — інакше власний
  хвіст передачі глушить квитанцію, яка починається вже через 30 мс (`RCTL_COMPAT.md` §5h).
- **Усі 4 команди RCTL працюють В ОБИДВА БОКИ, перевірено на залізі (2026-09-12):** форк
  як командир і як ціль для Radio Check / Monitor / Enable / Disable, з правильними
  квитанціями (Check/Enable/Disable — правило старшого біта; Monitor — власний формат
  `a0 10 9d 00`). Monitor-ціль відкриває прихований мік. Деталі — `RCTL_COMPAT.md` §5–§8e.
- **Квитанції Enable/Disable — знято з ефіру, реалізовано, хостові тести (2026-09-12).**
  Те саме правило (старший біт керуючого байта): Enable `a4 10 00 fe …`, Disable
  `a4 10 00 ff …`. Командир чекає підтвердження й на Enable, і на Disable — та сама
  галочка/хрест, звіряється команда квитанції. Формат узагальнено на всі команди
  (`dmr_rctl_stock_*_for()`); Monitor квитанції не має. Деталі — `RCTL_COMPAT.md` §6.
  **Лишилось перевірити на залізі:** галочка на Enable/Disable проти стокової.
- **Disable/Enable → справжній сон (stun):** заблокована рація виглядає вимкненою (чорний
  екран, тиша, клавіші й регулятор гучності не реагують), але приймач і обробка RCTL живі —
  Enable по ефіру її будить. Стан переживає перезавантаження (флеш). APO в сні не вимикає
  рацію. Аварійний вихід кабелем: `rctl_capture.py --unlock`. Усі сценарії перевірені.
  **NB (2026-09-12):** заблокований форк на Radio Check ВІДПОВІДАЄ квитанцією — як і стокова
  (перевірено: стокова у стуні теж тікає). Тобто галочка на Check заблокованої рації = «в зоні,
  розбудиться», а не «працює». Гейт дозволів діє й у сні; локальний «мертвий» вигляд лишається.
- Дозволи RCTL: пер-командні (Motorola/Hytera-модель). Відновлення після прошивки з ПК:
  `rctl_capture.py --rctl-on|--rctl-off|--rctl-status` (дозволи злітають при прошивці —
  це процес прошивки, не код; код збереження коректний, переживає вимк/увімк).
- Меню «Доступ RCTL»: 4 пункти (Перевірка/Моніторинг/Вимкнення/Ввімкнення), без зайвого
  майстер-перемикача; підказка під валкодер (↑/↓ рух, валкодер — On/Off).

**Інше:** попап «поза зоною ретранслятора» (правильний напис і час); S-метр; іконка та
екран вітання; захист anti-replay переживає вимкнення; інструмент захоплення сирих burst
(`rctl_capture.py --arm`, ловить будь-який data-sync burst, у т.ч. CRC-невалідний).

**Діагностика пам'яті збірки (2026-09-19) -- передумова до розвідки запису переговорів:**
- **Мета:** розвідка `docs/recording-feasibility.md` (2026-09-18) мала лише статичний
  здогад "вільної основної RAM правдоподібно достатньо" -- ця партія замінює здогад
  на інструментарій вимірювання (самі числа з'являться в першому CI-прогоні з цим
  бандлом -- ARM-тулчейн у цій пісочниці недоступний, тож реальний `.elf` тут не
  зібрати; див. `docs/recording-feasibility.md` §1.3 для деталей обмеження).
- **`.map`-файл збірки:** уже генерувався (`-Wl,-Map=...` у Makefile) -- нічого
  не змінювали, лише перевірили.
- **CI (`.github/workflows/build.yml`), усі три job'и збірки (build ENABLE_AES=0/1,
  build-ukrainian), одразу після `make`:**
  - новий крок `python3 tools/mem_report.py build/openuv380-10w.elf --top 20` --
    з готового `.elf` рахує зайнято/вільно окремо для RAM (128 КБ) і CCMRAM (64 КБ)
    (`arm-none-eabi-size -A`) і друкує топ-20 найбільших статичних об'єктів у кожній
    області (`arm-none-eabi-nm --format=sysv -S --size-sort`) з окремою перевіркою
    `screenNotificationBufData` (де він і скільки важить);
  - `.map`-файл кожної конфігурації вивантажується окремим артефактом запуску
    (`openuv380-10w-aes{0,1}-map`, `openuv380-10w-ukrainian-map`) -- поелементний
    перегляд без повторної збірки.
  - Зроблено для ВСІХ трьох job'ів матриці (не лише україномовної) -- саме різниця
    між ENABLE_AES/ENABLE_DMR_DATA конфігураціями й цікава.
- **`tools/mem_report.py` (новий):** парсить `size -A` і `nm --format=sysv` (не
  сирий текст `.map` -- крихкіше); `nm`-формат sysv узято свідомо, бо звичайні
  однобуквені класи типів `nm -S` (b/B) НЕ розрізняють `.bss` (RAM) і `.ccmram`
  (CCMRAM) -- обидві секції uninitialized+writable. `--self-test` перевіряє логіку
  розбору на вбудованих синтетичних зразках без ARM-тулчейну (ним і виявлено
  реальний баг перед доставкою -- див. нижче).
- **Рантайм-діагностика (дефайн `ENABLE_MEM_DIAG`, типово вимкнено, незалежний від
  ENABLE_AES/ENABLE_DMR_DATA -- потрібен і на "голій" конфігурації для порівняння):**
  новий опкод USB CPS `0xB4` (`usb_com.c`) віддає `xPortGetFreeHeapSize()`,
  `configTOTAL_HEAP_SIZE`, і `uxTaskGetStackHighWaterMark()` для `hrc6000TaskFunction`
  (задача кодека/AES) і для головної задачі (`applicationMainTask()` -- сама й обробляє
  цей запит, тож `uxTaskGetStackHighWaterMark(NULL)` без додаткового handle-пошуку).
  ПК-бік -- новий `tools/mem_diag.py` (патерн `tools/sms_diag.py`), з режимом
  `--watch <с>` для спостереження запасу під активним прийомом, не лише статичного
  знімка. Дрібний супутній рефактор без зміни поведінки: розмір стека
  `hrc6000Task` винесено з магічного числа `5000L/sizeof(portSTACK_TYPE)` в іменовану
  константу `HRC6000_TASK_STACK_WORDS` (`HR-C6000.h`), щоб не дублювати число між
  місцем створення задачі й новою діагностикою.
- **Виявлений і виправлений баг парсера (до доставки, самоперевіркою):** перша версія
  `parse_size_A()` пробувала розібрати розмір як hex, а при невдачі -- як decimal;
  але `size -A` завжди друкує розмір ДЕСЯТКОВИМ, а число на кшталт "2048" є коректним
  і як hex (0x2048=8264) -- тобто помилка мовчки давала ЗОВСІМ ІНШЕ число, а не падала
  з помилкою розбору. Спіймано власним `--self-test` (зразок `.data=2048` -> парсер
  віддав 8264) ще до того, як це потрапило в реальний звіт CI. Виправлено: `size -A`
  тепер парситься виключно як decimal (`parse_int_dec`), `nm --format=sysv` -- окремим
  hex-парсером (`parse_int_hex`), без спільної "спробуй і те, і те" логіки.
- **`syntax_check.sh`:** додано 4-ту конфігурацію матриці, `MEMDIAG`
  (`ENABLE_MEM_DIAG=1`, БЕЗ `ENABLE_AES`/`ENABLE_DMR_DATA` -- саме там, куди інші
  конфігурації не заглядають) -- увесь код тепер компілюється (хостовим gcc,
  `-fsyntax-only`) у 4 конфігураціях: AES0/AES1/UA/MEMDIAG, 368 файлів, 0 помилок.
- **Не реалізовано (свідомо, поза межами задачі):** сам функціонал запису переговорів
  -- лише вимірювання, як і просилось.
- **`docs/recording-feasibility.md` §1.3 і сценарій A оновлено** -- здогад замінено
  на опис готового інструментарію; остаточні числа -- з першого CI-прогону.

**Партія правок з фідбеку fleet (2026-09-18, восьма):**
- **#22 Попап ПОТУЖНОСТІ: прибрано проблиск S-метра поверх картки (у гучності його не було).**
  Симптом: під час показу повноекранної картки потужності на мить проскакував заголовок
  екрана VFO/каналу з S-метром — саме в момент натискання кнопки потужності.
  Причина: `uiUtilityRedrawHeaderOnly()` (`uiUtilities.c`) — функція, що перемальовує
  шапку/блок S-метра, — закінчувалась ПРЯМИМ `displayRenderRows()`, в обхід
  `displayRender()`, а отже й в обхід перевірки `uiNotificationIsVisible()`, яка вже
  захищає гучність/шумоподавлювач і звичайний RSSI-таймер (`displayRender()` сам
  перенаправляє на `uiNotificationRefresh()`, коли сповіщення видиме, замість фізичного
  пуша шапки). Обробник зміни потужності в `uiChannelMode.c` (усі чотири місця:
  довге натискання, коротке, обидва напрямки) кличе `uiUtilityRedrawHeaderOnly()`
  БЕЗПОСЕРЕДНЬО одразу після `increasePowerLevel()`/`decreasePowerLevel()` — тобто рівно
  в момент, коли попап потужності щойно показано — і той прямий виклик штовхав свіжу
  шапку на LCD, перекриваючи щойно намальовану картку. У VFO-режимі обробник потужності
  цього НЕ робить (лишає `headerRowIsDirty=true`, який іде через вже захищений шлях у
  `applicationMain.c`), тож у VFO симптом не виникав.
  Виправлено В ОДНОМУ МІСЦІ -- всередині самої `uiUtilityRedrawHeaderOnly()`: замість
  безумовного `displayRenderRows()` тепер той самий вибір, що й скрізь -- `displayRender()`
  при видимому сповіщенні (перемальовує картку), інакше звичайний частковий
  `displayRenderRows()`. Це заразом захищає ВСІ виклики функції, не лише з обробника
  потужності: гілку Dual Watch у періодичному RSSI-тіку `uiVFOMode.c` (там теж був прямий
  виклик) і гілку QSO caller-даних у `uiChannelModeUpdateScreen()`/`uiVFOModeUpdateScreen()`
  (обидві теж кликали функцію напряму). Ніякого окремого нового механізму не вводили --
  той самий, що й у гучності.
  Перевірено трасуванням коду (без ARM-тулчейну в цій пісочниці, тому без реального LCD):
  усі виклики `increasePowerLevel`/`decreasePowerLevel` у `uiVFOMode.c` і `uiChannelMode.c`
  тепер або йдуть через `headerRowIsDirty`→захищений консюмер, або через щойно виправлену
  `uiUtilityRedrawHeaderOnly()`; у самих `uiVFOModeUpdateScreen()`/`uiChannelModeUpdateScreen()`
  інших прямих (неперевірених) `displayRenderRows()`-викликів більше немає. **Натурну
  перевірку на залізі (утримання/швидке багаторазове перемикання потужності, у VFO і в
  каналі, при активному прийомі) лишаю на fleet** -- це рендер-таймінг, а хостові тести
  екран не симулюють.
  syntax_check (усі 3 конфігурації) + run_tests (9/9) + check_string_encoding +
  check_menu_widths -- усі чисті.

**Партія правок з фідбеку fleet (2026-09-18, сьома):**
- **#21 Виправлено регресію CI від коміту 90097cc (попап потужності) + зроблено, щоб такий
  клас поломки ловився локально наперед.**
  Симптом: коміт 90097cc зробив зеленим лише job `build (українська, ENABLE_AES=1
  ENABLE_DMR_DATA=1)`, а `build (ENABLE_AES=0)` і `build (ENABLE_AES=1)` падали.
  Причина: `uiNotification.c`, case `NOTIFICATION_TYPE_POWER`, безумовно звертався до
  `currentLanguage->power` — а це поле `stringsTable_t` оголошене (свідомо, щоб не
  зрушити розкладку структури для інших збірок) лише під `#if defined(LANGUAGE_BUILD_UKRAINIAN)`
  у `uiLanguage.h`. У збірках без цього прапорця поля просто немає.
  Виправлено: заголовок картки тепер бере `currentLanguage->power` лише за наявності
  `LANGUAGE_BUILD_UKRAINIAN`, інакше літерал `"Power"` (та сама умова, що й навколо самого
  поля). Розкладку `stringsTable_t` не чіпали — поле лишилось де було, останнім, під тим
  самим прапорцем. Перевірено: інших безумовних звертань до полів з-під мовних прапорців
  (`aes_keys`/`encrypt_tx`/`messages`/`remote_control`/`rctl_config`/`smeter_header`/
  `gps_absent`/`power`) у `uiNotification.c` немає — `currentLanguage->` там використано
  лише для `volume`/`squelch` (обидва не гвардовані) і тепер умовно для `power`.
  **Чому локальна перевірка це пропустила:** `syntax_check.sh` компілював КОЖЕН файл ОДНИМ
  жорстко заданим набором прапорців — саме тим, що в job `build-ukrainian`
  (`-DLANGUAGE_BUILD_UKRAINIAN` завжди ввімкнено), — тож помилка, що стосувалась лише
  конфігурацій БЕЗ цього прапорця, локально просто не виникала.
  Виправлено назавжди (а не лише для цього конкретного поля): `syntax_check.sh` тепер
  компілює кожен файл у ВСІХ ТРЬОХ конфігураціях матриці збірки (AES0/AES1/UA — прапорці
  рівно ті самі, що в трьох job'ах `build.yml`). Перевірено регресійно: тимчасове повернення
  безумовного звертання до `currentLanguage->power` одразу валить нову перевірку на AES0/AES1
  (`stringsTable_t' has no member named 'power'`) і проходить на UA — тобто новий клас
  перевірки дійсно ловить саме цей клас багів.
  `test_build.sh` (локальний білд-тест, не сам CI): прибрано глушіння `make clean`/`make`
  через `>/dev/null` — тепер вивід пишеться в тимчасовий лог, а при падінні збірки друкується
  хвіст логу (раніше при падінні в консолі не було жодного рядка помилки).
  `.github/workflows/build.yml`: оновлено коментар над job `checks`, щоб описувати нову
  поведінку `syntax_check.sh`.
  **Обмеження цієї перевірки:** ARM-тулчейн недоступний у пісочниці, де готувалась ця
  партія правок (мережа обмежена стандартними реєстрами пакетів; archive.ubuntu.com і
  завантаження ARM GNU Toolchain — 403). Тому справжню ARM-збірку (`make ENABLE_AES=0/1`,
  `make LANGUAGE_UKRAINIAN=1 ENABLE_AES=1 ENABLE_DMR_DATA=1`) тут не запускали — останнє
  слово, як завжди, за GitHub Actions після пушу бандла. Розширений `syntax_check.sh`
  (хостовий gcc, `-fsyntax-only`, усі 3 конфігурації) — це найближче, що можна перевірити
  локально без тулчейну, і саме він відтворив і підтвердив цей конкретний клас поломки.
  syntax_check + run_tests (9/9) + check_string_encoding + check_menu_widths — усі чисті.

**Партія правок з фідбеку fleet (2026-09-18, шоста):**
- **#20 Попап ПОТУЖНОСТІ переведено на той самий стиль, що й ГУЧНІСТЬ/ШУМОПОДАВЛЮВАЧ.**
  Симптом: потужність малювалась вузькою рамкою `displayDrawRoundRectWithDropShadow`
  по центру екрана, поверх вмісту VFO/каналу — перекривала смугу S-метра й обрізала
  показники праворуч; виглядала й поводилась інакше, ніж повноекранна картка гучності
  (баг #5, 2026-09-11).
  Малювання картки «заголовок + велике значення по центру + горизонтальна смуга-індикатор»
  винесено зі case-блоку ГУЧНІСТЬ/ШУМОПОДАВЛЮВАЧ у нову спільну статичну функцію
  `displayLevelCard(title, valueText, pct)` у `uiNotification.c` — ті самі кольори за темою
  (жовтий на темному вночі, темно-синій на білому вдень), шрифти, відступи; case ПОТУЖНІСТЬ
  тепер теж викликає її. Формат великого значення НЕ змінено — як і раніше, `getPowerLevel()`
  + `getPowerLevelUnit()` дають готовий рядок («500mW»/«1W»/«5W» тощо). Смуга для потужності
  показує позицію серед дискретних рівнів (0..`MAX_POWER_SETTING_NUM`=9 з `trxGetPowerLevel()`),
  а не відсоток від безперервного діапазону, як у гучності/шумоподавлювача — це осмислено різна
  подача при однаковій відмальовці. Керування (валкодер змінює рівень, таймаут закриває попап)
  не чіпали — лише сама відмальовка.
  Новий заголовок картки — окремий рядок мовної таблиці `power` (додано ОСТАННІМ полем
  `stringsTable_t`, під тим самим прапорцем `LANGUAGE_BUILD_UKRAINIAN`, що й `gps_absent` —
  щоб розкладка структури для збірок без цього прапорця лишилась байт-в-байт тією ж).
  `english.h`: «Power»; `ukrainian.h`: «Потужність» (правлено через Python із явним
  `cp1251`-кодуванням — файл не в UTF-8, Edit-інструмент його б спотворив).
  syntax_check + run_tests (9/9) + check_string_encoding + check_menu_widths — усі чисті.

**Партія правок з фідбеку fleet (2026-09-18, пʼята):**
- **#19 RCTL: прибрано майстер-перемикач «enabled» — єдина точка правди тепер маска команд.**
  Симптом: у прошивальнику стояло enabled=False, а в меню рації всі типи On — оператор
  бачив суперечливу картину, бо два поля (`enabled` і маска дозволів) могли розійтися.
  Причина: прошивальник писав `enabled` окремо (`SetEnabled`), а меню/утиліти — маску;
  ніщо не тримало їх узгодженими.
  Тепер `enabled` **похідний** від маски (`enabled = mask != 0`) і пишеться АВТОМАТИЧНО
  тим самим записом, що й маска (`dmrRctlConfigSetAllow`). Окремий `dmrRctlConfigSetEnabled`
  прибрано з прошивки, USB 0x9C і меню рації. Байт `enabled` лишився у структурі (довжину
  блока не міняли — тих самих 8 Б), просто більше ніхто не пише його незалежно.
  Гаряча гілка прийому НЕ змінилась: `dmrRctlAllowMask()` і далі спершу дивиться `enabled`,
  потім біт маски — жодної зміни поведінки в рантаймі.
  **Міграція вже прошитих рацій** (критично): рація, де `enabled` СВІДОМО вимкнули старим
  прошивальником при ненульовій масці (enabled=0, allow≠0), НЕ починає приймати команди
  після оновлення. Досягнуто тим, що нормалізація відбувається лише **на запис, ніколи на
  читання**: `cfg_load()` лишає `enabled` як у флеші, гаряча гілка мовчить (enabled=0), а
  меню/ПК показують ЕФЕКТИВНЕ значення (enabled && біт) — тобто все Off — і редагуються з
  чистого стану. Перший же свідомий запис нормалізує обидва поля. Прошивальник/меню пишуть
  саме ефективну маску, тож прихований дозвіл на «вимкненій» рації не воскресає.
  Меню рації «Доступ RCTL»: пункти ті самі; вмикання будь-якого типу авто-виставляє
  enabled=1, зняття останнього — enabled=0; окремого пункту «enabled» немає.
  Прошивальник, секція «Віддалене керування (RCTL)»: замість чекбокса «enabled» —
  чотири чекбокси типів команд (Ввімкнення/Перевірка/Моніторинг/Вимкнення, назвами як у
  меню рації); чекбокс видимості пункту меню лишився; «Прочитати стан» виводить маску по
  типах; текст угорі оновлено. `tools/rctl_config.py`: `build_payload` тепер виводить
  enabled з маски (параметр `enabled` прибрано), `parse_payload` віддає й ефективну, й сиру
  маску; `--enable/--disable` стали синонімами операцій над маскою, додано роботу `--allow`
  окремо. `rctl_capture.py` (0x9C) сумісний без змін: `--rctl-off`→(0,0)→mask 0→enabled 0,
  `--rctl-on <mask>`→enabled=(mask≠0).
  Тести: T7 переписано під `SetAllow` (без прибраного `SetEnabled`), T12/T14/T16 приведено
  до нової моделі, додано **T19** (узгодженість enabled↔маска у флеші) і **T20** (міграція
  legacy enabled=0/mask≠0: при завантаженні не оживає, читання не пише у флеш, перший запис
  нормалізує без воскресіння прихованого дозволу). Усі 9 тестів + syntax_check +
  check_string_encoding + check_menu_widths чисті.
  Майбутні типи команд: новий біт — у вільні біти наявних байтів (allow має 0x20/0x40/0x80,
  є цілий monitorSecs), не подовжуючи блок; дефолт у старому конфізі = 0 (заборонено).

**Партія правок з фідбеку fleet (2026-09-18, четверта):**
- **#17 Прошивальник: прибрано назву форка з рядка збірки.** У #16 помилково прибрав не те
  (секцію вибору прошивки) через обрізаний скріншот — за уточненням від реального
  скріншота цілий був напис «OpenGD77-AES256-fork» у рядку збірки. Прибрано з двох місць:
  генератора `build_info.txt` у CI (`.github/workflows/build.yml`) і захисно при
  завантаженні в самому `gui_flasher.py` (`read_build_info()` обрізає цей префікс і зі
  старих уже зібраних `.exe`, де `build_info.txt` лишився в старому форматі).
- **#18 Кнопка «Віддалене керування» ще й ховає/показує пункт меню «Доступ RCTL».** Раніше
  вікно RCTL у прошивальнику вміло лише enabled/disabled; тепер другий чекбокс —
  видимість самого пункту меню на рації (для флоту: боєць не мусить бачити чи чіпати
  цей пункт узагалі). Це САМЕ вигляд — гейт дозволів (allow-маска) працює незалежно,
  схована рація однаково виконує лише дозволені команди.
  На прошивці: `dmrRctlOnFlashCfg_t` (dmr_rctl_cfg.c) **не подовжено** — біт видимості
  займає верхню половину вже наявного байта `allow` (0x10), а не нове поле. Причина:
  і прошивка (`codeplugSetOpenGD77CustomData`), і ПК-утиліта (`custom_data.py`)
  **забороняють міняти довжину вже існуючого блоку** custom-data — на рації, де RCTL уже
  хоч раз вмикали, будь-який запис довшого блоку просто мовчки провалився б (`ok=0`), і
  фіча не запрацювала б на жодній уже налаштованій рації. Верхня половина `allow` на
  будь-якому блоці, записаному старим кодом, завжди була нульовою (`allow` завжди
  маскувався через `ALLOW_ALL=0x0F`), тож старий блок автоматично читається як «пункт
  видимий» — без міграції розміру чи даних. Знайдено й виправлено до польового тесту,
  тестом `T18` у `tests/test_dmr_rctl_cfg.c` (round-trip запису в уже існуючий
  8-байтовий блок v4, перевірка, що дозволи/monitorSecs не губляться при зміні видимості
  й навпаки).
  ПК-сторона: `tools/rctl_config.py` (був застарілим — VERSION=3, не знав про
  `monitorSecs`/вигляд; додано `--hide-menu`/`--show-menu`, версія/довжина блоку
  приведені до фактичного формату прошивки v5) і `tools/gui_flasher.py`
  (RctlConfigWindow: другий чекбокс + читання/запис через оновлений `rctl_config`;
  заразом виправлено `build_payload(enabled)` — виклик з одним аргументом падав би, бо
  сигнатура давно вимагає ще й `allow`).

**Партія правок з фідбеку fleet (2026-09-13, третя):**
- **#15 Банер SMS: будить екран, великий шрифт, без «RED».** Додано `displayLightTrigger()`
  перед показом — раніше банер міг намалюватись у темряву й лишитись непоміченим
  (підсвітка зазвичай погашена). Текст тепер `FONT_SIZE_3` (font_8x16): ширина символу та
  сама (19 у рядок), але рядків удвічі менше, тож підказку «RED» прибрано — вона з'їдала
  цілий рядок. Якщо текст не влазить — у кінці **три крапки** (одна читалась би як звичайна
  крапка). Рядок «ще N непрочит.» лишився дрібним і з'їдає місце лише коли N > 0.
  Звірено піксельним рендером `tools/render_sms_banner.py` (справжні гліфи UA-набору +
  справжні кольори тем, логіка переносу — дзеркало `displaySmsFullScreen()`).
- **#16 Прошивальник: прибрано секцію вибору прошивки, додано автора.** Коли прошивка
  вбудована в `.exe` (штатний випадок для флоту), секції немає **зовсім** — ні назви файлу,
  ні кнопки: оператору нема чого обирати, а зайвий вибір лише дає шанс залити не те.
  Секція не пакується (а не ховається), тож порожнього місця не лишається. Нумерацію кроків
  прибрано разом із нею — самотнє «1.» виглядало б як загублений крок. Запобіжник: якщо
  вбудованої прошивки немає (запуск із джерел), секція з'являється, інакше шити було б
  нічим. Простежуваність не втрачено — журнал і далі пише «Починаю прошивку: <файл>».
  Додано рядок «Автор: UW5ELK» під підписом збірки.

**Партія правок з фідбеку fleet (2026-09-12, друга):**
- **#12 Банер вхідного SMS — на весь екран і до RED.** Був звичайний банер: 3–4 рядки по
  14 символів у рамці, гаснув за 4 с — для повідомлення до 144 символів замало, а якщо
  рація лежала в розвантажці, воно зникало непоміченим. Тепер окремий тип сповіщення
  `NOTIFICATION_TYPE_SMS`: заливка на весь екран, рамка, перенос **по словах**
  (19 символів × 14 рядків = до 266 символів, тобто повне SMS влазить), підказка «RED»
  унизу. Сам не гасне (`uiNotificationHasTimedOut` повертає false для цього типу) —
  закриває оператор червоною кнопкою, і те саме натискання «з'їдається», щоб RED заразом
  не вийшов з поточного екрана.
  Банер завжди несе **останнє** повідомлення, а щоб решта черги не лишалась невидимою —
  рядком нижче «ще N непрочит.» **кольором попередження**
  (`THEME_ITEM_FG_WARNING_NOTIFICATION`), тобто візуально окремо від тексту самого
  повідомлення. Рядок з'являється лише коли N > 0. Лічильник дешевий — прохід по масиву
  в CCM-RAM, у флеш не лізе, тож безпечний у шляху малювання.
- **#13 «Від. керування» → «Адміністрування».** Мовний файл (cp1251); ширину звірено
  `check_menu_widths.py` — переповнень немає.
- **#14 Лічильник непрочитаних не ріс понад 9.** Стояло жорстке `(unread > 9) ? 9 : unread`,
  тож 10 і 40 непрочитаних виглядали однаково. Тепер справжнє число (до 99), а конверт
  зсувається ліворуч рівно на ширину цифр, щоб двозначне не вилізло за край.

**Партія правок з фідбеку fleet (2026-09-11):**
- **#1 Фікс. PTT (залипання).** Стокова логіка вмикала залипання лише за заданого TOT на
  каналі, тож на каналах без TOT воно не працювало. Тепер вмикається завжди; для каналів
  без TOT — запобіжний таймаут 120 с (авто-зняття) з коротким сигналом. Перевірено на залізі. ✅
- **#2 Порожні зони.** Перемикання зон валкодером І список у меню «Зони» пропускають порожні
  (без каналів); «Всі канали» завжди видно. ✅
- **#4 GPS з меню.** Пункт GPS прибрано і з головного меню, і з «Загальних опцій» (рація без
  GPS-приймача); HAS_GPS/APRS лишились через `FORK_NO_GPS_MENU`. ✅
- **#5 Вікно гучності/шумоподавлення.** Повноекранне: заголовок, велике число у відсотках,
  смуга. Кольори за темою: НІЧ -- яскраво-жовтий на темному; ДЕНЬ -- темно-синій на білому. ✅
- **#6 Годинник + заряд разом.** Раніше при увімкненому годиннику заряд не показувався взагалі
  (ділили один правий кут). Тепер годинник праворуч, заряд ліворуч від нього. Перевірено. ✅
- **#7 APRS-прийом після бікону.** DMR RX-стан скидається після кожного бікону навіть без QSY
  (зайнятий канал + PTT більше не «вішає» прийом). Потребує польової перевірки.
- **#8 Ключі AES.** Прибрано Global TX key; шифрування береться лише з каналу; стандарт=вимкнено;
  порядок Off, Key 1…; прибрано inherit. ✅
- **#9 Літера «ю».** Гліф [254] font_8x16 перемальовано (стійка + перемичка + чисте кільце),
  без злипання з сусідньою «ч». Перевірено на залізі. ✅
- **Назва прошивки OpenGD77 → OpenTYT** (екран «Прошивка»/firmware info та голосова підказка).
- Верстку #5/#6/#9 звірено піксельним рендером (`tools/render_screen.py`-стиль: реальні шрифти
  UA-набору + реальні кольори тем).

## Розвідка (нічого не реалізовано)

- **Запис переговорів (call recording) -- технічна розвідка (2026-09-18).** Питання
  від fleet: чи можна додати запис, як у стоковій TYT. Повний звіт --
  `docs/recording-feasibility.md` (карта SPI-флеша й скільки вільно, окремого
  флеш-чипа під запис не знайдено, голосовий тракт RX/TX/відтворення й межа
  AES-шифрування, чому аналоговий FM-запис неможливий на цьому залізі, оцінка
  вільної RAM). Три сценарії з цифрами й ризиками: (A) кільцевий буфер у RAM --
  переслухати останній виклик (30-60 с, ~13-27 КБ) -- **можливо, з умовою**
  (2026-09-19: діагностика вільної пам'яті додана -- `tools/mem_report.py` в CI +
  рантайм-опкод `0xB4`/`tools/mem_diag.py`, деталі в партії правок нижче -- лишилось
  дочекатись числа з CI-прогону); (B) архів на флеш -- **можливо, з умовою** (~5 хв
  без жодних змін; години -- лише після окремої зміни, що обмежить стелю бази
  DMR-ID, зараз необмеженої); (C) вивантаження на ПК через наявний CPS/USB-протокол
  -- **можливо**, ~6.8 КБ/с оцінка знизу, готовий патерн уже є
  (`tools/aes_key_store.py`). Рекомендовано сценарій A як перший крок -- найменший
  обсяг роботи, перевикористовує вже наявний тракт відтворення голосових підказок
  (`voicePromptsTick()`/`codecDecode()`), не чіпає флеш-розмітку. Код не змінювався
  -- це розвідка, коміт лише з документом.

- **Буфер `screenNotificationBufData` (62% CCM) -- технічна розвідка
  (2026-09-19).** Привід: перший CI-прогін із `tools/mem_report.py`
  (35418546022) показав межову пам'ять -- вільно лише 212 Б RAM / 208 Б CCM
  (українська збірка). Найбільший одиночний споживач CCM --
  `screenNotificationBufData`, 40 960 Б = 62.5% усієї CCMRAM. Повний звіт --
  `docs/notification-buffer.md`: буфер -- це повноекранна копія живого
  кадрового буфера (не окрема поверхня картки), успадкована з апстріму
  OpenGD77 (комміт `ccd25f0`, форк його не додавав), пишеться/читається лише
  двома `memcpy` в `uiNotificationRefresh()`; DMA до нього ніколи не
  торкається (CCM апаратно не видна DMA1/DMA2 на STM32F405), тож версія
  "CCM заради DMA" не підтвердилась. Три варіанти: (a) зберігати лише
  прямокутник картки -- **звільняє 0 Б**, бо SQUELCH/VOLUME/POWER
  (`displayLevelCard()`) і SMS-банер і так на весь екран (фідбек fleet
  2026-09-11/18, відкат -- регрес UX); (b) не зберігати нічого,
  перемальовувати екран при закритті через уже наявний
  `menuSystemCallCurrentMenuTick()` (той самий шлях, що й у
  `daytimeThemeApply()`) -- **звільняє всі 40 960 Б**, обсяг робіт середній
  (~8 місць виклику `uiNotificationHide()` потребують індивідуальної
  перевірки), головний ризик -- фон (годинник/S-метр) "замерзає" на екрані
  під час ДОВГИХ `MESSAGE`-сповіщень із частковим перекриттям, найгірший
  випадок -- 60-секундне попередження APO (потребує рішення fleet і
  перевірки на залізі); (c) перенести буфер у звичайну RAM -- **відкинуто
  одразу**, арифметика: вільно там лише 212 Б, перенесення 40 КБ миттєво
  ламає лінковку. **Рекомендовано варіант (b)** -- з великим запасом
  вистачить і на кільцевий буфер запису сценарію A (13.5-27 КБ,
  `docs/recording-feasibility.md`), причому просто в CCM, не в основній
  RAM, як передбачав той документ раніше. Побічно (п.5 звіту): рантайм-опкод
  `0xB4` зараз показує лише ПОТОЧНЕ вільне місце в купі FreeRTOS, не
  МІНІМАЛЬНЕ за весь час роботи (`xPortGetMinimumEverFreeHeapSize()` існує
  в `heap_4.c`, але опкодом не читається) -- зафіксовано як можливий
  другий резерв, без рекомендацій (потребує польового спостереження, а не
  одного знімка). Код не змінювався -- це розвідка, коміт лише з
  документом.

## НЕ зроблено / відкладено

1. ~~**ACK (квитанція «онлайн» у командира)**~~ — **ЗРОБЛЕНО, перевірено на залізі
   (2026-09-11).** Обидва напрямки працюють: стокова бачить наш форк онлайн, і форк показує
   зелену галочку на стокову. Деталі формату й реверсу — `RCTL_COMPAT.md` §5a–§5h.

2. ~~**Monitor як ціль (прихований мікрофон)**~~ — **ЗРОБЛЕНО, перевірено на залізі**
   (2026-09-12). На дозволений Monitor форк шле квитанцію `a0 10 9d 00 <командир> <ціль>`
   (власний формат монітора, знято з ефіру — НЕ правило біта; §8e) і відкриває мік:
   приватний голосовий виклик тому, хто запитав, повний час. Приховано (без LED/екрана/
   бліпів), шифр ключем каналу, тривалість на рації (меню «Доступ RCTL» → «Час моніт.»,
   10–120 с). Гейт ALLOW_MONITOR; не перебиває чужу передачу; PTT оператора обриває. Межа:
   лише власний флот. Деталі — `RCTL_COMPAT.md` §7–§8e.
3. ~~**Radio Check як ціль**~~ — **ЗРОБЛЕНО** (п.1). Форк відповідає на дозволений Check
   стоковою квитанцією; заблокований (stun) форк на Check теж відповідає — як заводська
   (§6a: стокова у стуні не мовчить). Локальний «мертвий» вигляд рації лишається.
4. **Дрібне на перевірку:** витіснення кеша anti-replay при 8+ різних командирах; побайтна
   звірка запису AES-ключів з ПК; `gui_flasher` — не валідований наскрізно.
5. **#11 Формати SMS + повна квитанція (ACK).**
   - **АКТИВНО (2026-09-12): напрямок TX — «доставлено» на НАШОМУ форку.** Гілку «щоб
     стокова зарахувала нашу квитанцію» зупинено (висновок вище). Розворот: стокова
     **вміє надсилати** валідні квитанції (одну знято з ефіру й декодовано, RT4D її
     зарахував), тож якщо форк шле власні SMS як **CONFIRMED**, стокова відповість
     по-справжньому, а форк покаже «доставлено». Тут докази за нас.
     Трелісний кодер НЕ потрібен: підтверджені блоки бувають і rate-1/2 (12 байт = 2
     службові + 10 навантаження), а rate-1/2 ми вже вміємо. Нове одне — **CRC9**.
     Крок 1 (зроблено): дамп СИРИХ блоків стокової разом зі службовими байтами
     (`dmrSmsRxRawBlocks`, USB 0xB3, `sms_diag.py --blocks`) — еталон, щоб реверснути
     CRC9 по реальних даних, а не вигадувати (урок квитанції).
     Крок 2: звірити CRC9 офлайн по цих блоках → зібрати CONFIRMED-TX (заголовок DPF=3
     з бітом A + блоки з DBSN/CRC9) → читати вхідну `01 40` → позначка «доставлено».
   - **Формат сумісності = «M» (Motorola).** Наш формат = IPv4/UDP/TMS, SAP=4 (IP based packet
     data) — підтверджено декодом ефіру (BBD_0005). У CPS стокової TYT це пункт **`M`** (не
     `DMR`, що є ETSI UDT — інша схема, форк її не розуміє). На RT4D — `Motorola`. Тобто для
     взаємодії форк ↔ стокова ↔ RT4D усі три ставлять Motorola/M-формат, однакові
     контакт/TG/CC/слот.
   - **Квитанція, напрямок RX (хтось → форк): ЗРОБЛЕНО** (див. «Зроблено», розділ SMS). Форк
     тепер квитує вхідне CONFIRMED SMS `01 40 …` — відправник бачить «доставлено».
   - **Квитанція, напрямок TX (форк → хтось): ЛИШИЛОСЬ.** Щоб форк-ВІДПРАВНИК бачив
     «доставлено», треба: (а) слати власні повідомлення як **CONFIRMED** (DPF=3, біт A=1) з
     блоками **rate-3/4** (18-байтний блок = DBSN+CRC9 + 16 навантаження) — потрібен rate-3/4
     кодер (наразі є лише декодер) + треліс-кодування; (б) приймати вхідну `01 40` на нашу
     адресу й ставити «доставлено» біля відправленого. Це більший шматок; квитанцію-читач
     (б) можна додати дешево, але без CONFIRMED-TX (а) її нічим викликати.
   - Hytera-формат — окремо, за потреби; знімати з ефіру HackRF-ом так само.
   - Інструмент реверсу: `tools/dmr_data_rx.py` (C16 → data-burst-и BPTC(196,96) + CRC-маски
     0x5A5A CSBK / 0x3333 data-header). Ним і декодовано BBD_0005.

## Робочий процес (нагадування)

**Середовище користувача: Termux на Android.** Репозиторій — `~/OpenGD77-AES256`
(НЕ `~/OpenGD77-AES256-fork`). Завантажені файли лягають у `/sdcard/Download/`
(Android, «Download» без «s»); теки `~/Downloads` НЕ ІСНУЄ — не пропонувати її.
Пуш у GitHub робить користувач з телефона (свій SSH-ключ); з хмарного середовища
github.com заблоковано проксі — обхід тільки через `git bundle`.

Готовий блок для застосування бандла (сам знаходить найсвіжіший, шлях вводити не треба):

```
cd ~/OpenGD77-AES256 && \
B=$(ls -t /sdcard/Download/*.bundle 2>/dev/null | head -1) && \
echo "Бандл: $B" && git pull "$B" main && git push
```

- Прошивка форка: блок вище → зелений CI → прошити `.sgl` →
  залити AES-ключі з ПК → `rctl_capture.py --rctl-on` (відновити дозволи RCTL).
- Тести RCTL: `rctl_capture.py --rxdiag` (лічильники прийому), `--rctl-status` (дозволи),
  `--unlock` (кабельне пробудження зі сну).
