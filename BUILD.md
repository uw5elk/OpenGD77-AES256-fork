# Збірка та прошивка прошивки MD-UV390 (10W, AES256)

На основі офіційного релізу вихідного коду OpenGD77 **R20260131** (`OpenGD77_MDUV380_DM1701_20260130`),
налаштовано для збірки цілі **MD-UV390 Plus 10W** (`MDUV380_10W_PLUS_FW` = `PLATFORM_MDUV380` +
`PLATFORM_VARIANT_UV380_PLUS_10W`) з командного рядка / CI, без STM32CubeIDE.

Цей реліз включає апаратні виправлення для 10W Plus: сигнали керування екраном/підсвіткою та PA
переставлені місцями порівняно з 5W-раціями, а новіші виробничі партії мають LCD з низькою
яскравістю — усе це враховує ціль 10W_PLUS. **НЕ прошивай 5W-збірку в 10W-рацію.**

## TL;DR наскрізь (один скрипт)
```sh
# 1. встанови офіційний тулчейн ARM GNU (14.2.Rel1) і python-залежності:
pip install -r MDUV380_firmware/tools/requirements.txt
# 2. переведи рацію в режим DFU/завантажувача, тоді:
./flash.sh /path/to/MD9600-CSV2571V5-V26.45.bin        # додай 'aes' для експериментальної AES-збірки
```
`flash.sh` збирає прошивку, перевіряє донора кодека, вбудовує кодека AMBE і прошивкає рацію через DFU.

## Донор кодека AMBE (потрібен для DMR)
DMR використовує пропрієтарний вокодер AMBE+2 (ліцензія DVSI), якого OpenGD77 не може
 розповсюджувати. Ні вихідний код, ні ЖОДЕН зібраний р.bin` (ні наш, ні офіційні релізи
OpenGD77) не містить його — лінкер резервує порожню область (`.codec_bin_section_1`,
`0x48BB0` байт за адресою flash `0x0807537C`, генерується автоматично `codec_cleaner` під
час збірки й перевіряється на 100% нулі). Кодек вбудовується з **донора** під час прошивки.

Донором має бути **MD-9600 V5**: `MD9600-CSV(2571V5)-V26.45.bin` (офіційна прошивка TYT
MD-9600). MD-9600 має той самий процесор STM32F405 і ідентичний кодек AMBE, що й MD-UV380/390,
тому OpenGD77 використовує його як універсальне джерело кодека. **Прошивки MD-UV390 AES НЕ є
валідними донорами** (неправильне зміщення кодека). Перевіряй будь-якого кандидата:
```sh
python3 MDUV380_firmware/tools/verify_codec_donor.py <donor.bin>     # виводить VALID / INVALID
```

## Ручна збірка
```sh
cd MDUV380_firmware
make -j$(nproc)                 # стокова; або: make ENABLE_AES=1
# -> build/openuv380-10w.bin  (~806 КБ: код + зарезервована область кодека)
```
`Makefile` дублює конфігурацію релізу CubeIDE `MDUV380_10W_PLUS_FW` (-Os, `-mcpu=cortex-m4
-mfpu=fpv4-sp-d16 -mfloat-abi=hard`, визначення `USE_HAL_DRIVER STM32F405xx PLATFORM_MDUV380
PLATFORM_VARIANT_UV380_PLUS_10W`, лінкер `STM32F405VGTX_FLASH.ld`) і автоматично створює
порожній плейсхолдер кодека через `tools/codec_cleaner.Linux -C`.

## Ручна прошивка
```sh
cd MDUV380_firmware
python3 tools/opengd77_stm32_firmware_loader.py -s <donor.bin> -f build/openuv380-10w.bin -m MD-UV380
```
Завантажувач вбудовує кодек AMBE в зарезервовану область, зливає мовні пакети, тоді завантажує
через DFU. Виводить **"Patching for DMR"** при успіху (або **"Flashing FM Only firmware"**, якщо
донора нема чи він невалідний).

## Тест збірки
```sh
./test_build.sh [опційний-донор.bin]   # збирає обидва варіанти, перевіряє готовність кожного до злиття кодека
```
CI (`.github/workflows/build.yml`) робить те саме для `ENABLE_AES` 0 і 1 і перевіряє розмір,
готовий до злиття.

## AES-збірка
`ENABLE_AES=1` вбудовує DMRA (сумісне з Motorola/Hytera) шифрування голосу AES-256 — і
**передачу, і прийом**, сумісне зі стоковою TYT MD-UV390 ("Universal" AES256). Типова збірка
побайтово ідентична стоковому OpenGD77 (без символів AES). Криптографія лежить у
`application/source/crypto/` (протестована юніт-тестами на хості: FIPS-197 AES, LFSR128d,
потік ключа OFB, Golay(24,12) late-entry MI, EMB BPTC single block); хуки HR-C6000 і кодека — в
`HR-C6000.c` і `dmr_codec/codec_interface.c`. Опційна діагностика, вимкнена за замовчуванням,
компілюється за прапорцями `DMR_AES_DIAG_RX` / `DMR_AES_DIAG_PATTERN` / `DMR_AES_DIAG_ENCPAT`
(наприклад, `make ENABLE_AES=1 DMR_AES_DIAG_RX=1`, читається через `tools/rxdiag.py` /
`tools/fragcap.py`). Зашифрований голос — лише для ліцензованого комерційного/PMR використання;
на аматорських діапазонах у більшості країн це незаконно.

## Апстрім / ліцензія
OpenGD77 від Roger Clark VK3KYY та учасників — див. `MDUV380_firmware/tools/license.txt` і
заголовки апстріму (стиль BSD-3-clause, лише некомерційне використання). Цей форк зберігає всі
файли й заголовки авторських прав апстріму.

## Доступ до USB для прошивки (за платформою)

Рація прошивається через USB DFU (завантажувач STM32, VID:PID `1fc9:0094`). Доступ до USB
відрізняється залежно від хоста:

- **Linux (нативно):** встанови вбудоване правило udev, що був DFU був доступний без root:
  ```sh
  sudo cp MDUV380_firmware/tools/udev/99-gd77.rules /etc/udev/rules.d/
  sudo udevadm control --reload-rules
  ```
- **Windows:** запускай завантажувач через Python + libusb, або використовуй OpenGD77 CPS.
  Інтерфейсу DFU потрібен драйвер WinUSB/libusb (наприклад, через Zadig), прив'sязаний до
  `1fc9:0094`, поки рація в режимі завантажувача.
- **WSL2:** USB не прокидується за замовчуванням — підключи пристрій через `usbipd-win`
  (`usbipd bind`/`usbipd attach --wsl`) перед запуском `flash.sh`, або просто прошивай з Windows.

Переведи рацію в режим DFU/завантажувача (увімкни, утримуючи бічні кнопки — див. інструкцію з
встановлення OpenGD77) перед запуском `flash.sh` / завантажувача.

## Статус

**Перевірено на залізі (MD-UV390 10W Plus):**
- Збирає офіційний вихідний CI код R20260131 для цілі 10W; результат структурно ідентичний
  офіційному релізу `OpenMDUV380_10W_PLUS.bin` (слот кодека, розкладка `.data`/`.ccmram`,
  мовні таблиці, розмір) — єдині побайтові відмінності це тулчейн/оптимізація й вбудований
  рядок дати збірки прошивки.
- Готовий до злиття кодека (806 КБ); кодек AMBE з донора **MD-9600 V5** вбудовується в
  зарезервований слот 0xFF за адресою `0x0807537C`, не чіпаючи `.data`/`.ccmram`.
- **AES-256 TX і RX сумісні зі стоковою TYT MD-UV390** ("Universal" AES256): стокова рація
  декодує зашифровані передачі цієї прошивки в чистий голос, а ця прошивка розшифровує
  зашифрований голос стокової рації — включно зі швидкими повторними викликами (приймач
  блокується на MI нового виклику через вбудований Late-Entry). Звірено із захопленнями
  HackRF + DSD-FME.
- Сховище ключів/значень AES зберігається постійно в області кастомних даних SPI-flash
  (переживає перезавантаження й перепрошивку); завантажується через хост-утиліту нижче.

## Завантаження AES-ключа (хост-утиліта)
З рацією, підключеною через USB (вона визначається як серійний порт OpenGD77 CPS, VID:PID
`1fc9:0094`):
```sh
pip install -r MDUV380_firmware/tools/requirements.txt
python3 MDUV380_firmware/tools/aes_key_store.py --key <64-hex-chars> --keyid 1 [--tx-key 1] [--port COM4]
```
`aes_key_store.py` записує ключ в область кастомних даних OpenGD77
(`CODEPLUG_CUSTOM_DATA_TYPE_AES_KEYS`) через той самий протокол прошивки CPS, що й сам CPS,
тому він переживає перезавантаження й перепрошивку. Прошивка читає його лениво при першому
зашифрованому виклику. `--tx-key N` вибирає, яким ключем передавати (`0` = передача без
шифрування вимкнена); `--show` виводить збережений блок. Меню введення ключа прямо на рація —
запланована альтернатива.
