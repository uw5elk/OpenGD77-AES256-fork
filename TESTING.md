# Протокол перевірки на залізі

Це те, що **не можна перевірити з комп'ютера**. Хостові тести й CI ловлять помилки в
логіці й у форматах, але вони нічого не знають про ефір, таймінги DMR і про те, як
насправді виглядає екран.

Найважливіше зараз: **дистанційне керування (RCTL) не перевірялось на реальних раціях
жодного разу**, а за останній тиждень його змінювали двічі — додали роботу на відкритих
каналах і окремі дозволи на кожну команду. До цієї перевірки будь-яке твердження, що
воно працює, — здогад.

> **Як користуватись.** Проходь блоками зверху вниз. Кожен крок має графу «має бути».
> Якщо збіглось — став `+`. Якщо ні — **сфотографуй екран** і запиши, на якому саме
> кроці. Часткові результати теж цінні: краще пройдені блоки A–C, ніж нічого.

---

## 0. Що знадобиться

- **Дві рації** MD-UV380/390 з цією прошивкою (однакова збірка, обидві оновлені).
- **Секундомір** — блок D вимірює час, і це не формальність (див. нижче).
- Аркуш або нотатки в телефоні для таблиці результатів наприкінці.

### ⚠️ Чи можна взяти другу рацію зі стоковою прошивкою TYT

| Блок | Стокова друга рація |
|---|---|
| **A, B, C, D, E** (RCTL) | ❌ **Ні.** RCTL — власний протокол цього форку; стокова рація його не розуміє й ніколи не відповість |
| **F** (SMS) | ✅ **Так, і це навіть цінніше** — див. нижче |
| **G** (тексти на екрані) | ✅ Друга рація взагалі не потрібна |

**Найважливіше застереження.** Зі стоковою рацією блоки A–E дадуть `Немає відповіді` на
**кожному** кроці. А кроки **C1 і C2 саме цього й очікують** — тобто вони «пройдуть»,
не перевіривши нічого. Це гірше за провал: провал видно, а хибне «+» тихо закриває
питання, яке лишається відкритим. Тому: **або обидві рації на цій прошивці, або блоки
A–E не проходяться взагалі** — краще прочерк, ніж вигаданий плюс.

Вихід, якщо друга рація позичена: прошивку **можна відкотити назад на стокову**. Перед
першим переходом збережи оригінальну прошивку й кодплаг (див. `BUILD.md`) — тоді рацію
можна повернути власнику в тому ж стані.

**А от для SMS стокова рація краща за нашу.** Наш формат — це реверс-інжинірений
стоковий TYT (AES-256-ECB над IPv4/UDP/TMS, ENC-заголовок Motorola ALG05), і сумісність
із заводською рацією підтверджувалась в ефірі. Дві рації на нашій прошивці перевіряють
лише те, що ми сумісні самі з собою; стокова перевіряє те, чого пара форків перевірити
не може в принципі. У блоці F стокова рація в ролі **A** (відправник) — це найсильніший
варіант тесту.

Голос і AES-256 зі стоковою вже перевірені на залізі (`BUILD.md`, розділ «Статус»),
тож повторювати їх не обов'язково.

Прошивку бери з CI: вкладка **Actions → останній зелений запуск → артефакт
`openuv380-10w-ukrainian`**, або GUI-прошивальником `TYT MD-UV390UKR.exe`
(артефакт `openuv380-flasher-windows`).

⚠️ **Про ефір.** Радіоперевірка — це справжній вихід в ефір обох рацій. Обирай частоту,
на якій маєш право працювати, і став **мінімальну потужність** — рації все одно поруч.

---

## 1. Підготовка (робиться один раз)

Далі рації називаються **A** (та, що перевіряє) і **B** (та, що відповідає).

1. На обох: **Меню → Про рацію** — запиши **DMR ID** кожної (він же в **Налаштування → Загальні → Мій DMR ID**).
   `ID A = ____________`  `ID B = ____________`
2. Обидві на **один цифровий канал**: однакова частота, талкгрупа, таймслот, колір-код.
3. Переконайся, що звичайний голос між ними проходить (натисни PTT на A — B має
   відкритись). **Якщо голос не проходить — далі йти немає сенсу**, проблема в
   налаштуванні каналу, а не в прошивці.
4. Заведи **два канали**: один **без** шифрування (`Шифр. TX` = Off) і один **з**
   ключем — той самий слот ключа на обох раціях. Ключі пишуться через
   **Меню → Налаштування → Ключі AES**.

---

## 2. Блок A — радіоперевірка на **відкритому** каналі

Це те, чого раніше не було взагалі: до 2026-09-05 без AES-ключа RCTL не працював.

**Налаштування перед блоком.** На **обох** раціях: **Меню → Налаштування → Доступ RCTL**
→ `Доступ` = **On**, `Перевірка` = **On**, решта **Off** → **зелена** (зберегти).

| № | Дія | Має бути |
|---|---|---|
| A1 | Обидві на **відкритий** канал. На A: **Меню → Від. керування**, набери **ID B**, **зелена** | На A з'являється `Перевірка...` |
| A2 | Дивись на екран A | Через ~1–2 с: спливає `Радіоперевірка: ID <B>`, під ним екран `Рація на зв'язку` і `ID <B>, 0 с тому` |
| A3 | **Одночасно дивись на екран B** (краще вдвох, або зніми відео) | На B — **нічого**. Ні напису, ні звуку, ні підсвітки. Перевірка має бути повністю тихою |
| A4 | На A: **SK1** замість набору цифр, обери B зі списку контактів, **зелена** | Той самий результат, що й A2 |

> Якщо A2 дає `Немає відповіді` — перш ніж шукати причину, пройди **A5** і блок C:
> найімовірніша причина — дозволи на B, а не радіочастина.

| № | Дія | Має бути |
|---|---|---|
| A5 | На A набери **неіснуючий ID** (напр. `9999999`), **зелена** | Через **8 секунд** — `Немає відповіді` |

---

## 3. Блок B — те саме на **шифрованому** каналі

| № | Дія | Має бути |
|---|---|---|
| B1 | Обидві на канал **з ключем**. Повтори A1–A3 | Точно той самий результат, що й на відкритому |
| B2 | На B прибери ключ каналу (`Шифр. TX` = Off), A лиши на шифрованому. Перевір з A | `Немає відповіді` — B не може прочитати команду |
| B3 | Поверни ключ на B, перевір ще раз | Знову працює |

---

## 4. Блок C — дозволи (**найновіше, ніде не перевірене**)

Це та сама модель, що в Motorola/Hytera: вирішує **сама цільова рація**, окремим
дозволом на кожну команду. Перевіряємо, що дозвіл справді щось означає.

Усе робиться на **B**, у **Меню → Налаштування → Доступ RCTL**. Після кожної зміни —
**зелена** (зберегти), інакше зміна не застосується.

| № | На B виставити | З A перевірити B | Має бути |
|---|---|---|---|
| C1 | `Доступ` = **Off** | перевірка | `Немає відповіді` |
| C2 | `Доступ` = **On**, `Перевірка` = **Off** | перевірка | `Немає відповіді` |
| C3 | `Доступ` = **On**, `Перевірка` = **On** | перевірка | `Рація на зв'язку` |
| C4 | `Доступ` = **On**, `Перевірка` = **On**, `Моніторинг`/`Вимкнення`/`Ввімкнення` = **On** | перевірка | `Рація на зв'язку`, і **більше нічого не відбувається** — ці три команди ще не реалізовані, дозвіл на них ні на що не впливає |
| C5 | Змінити щось і вийти **червоною** | зайти в меню ще раз | Зміна **не збереглась** — так і має бути |

**C2 — ключовий крок.** Саме він відрізняє нову модель від старої: раніше був один
перемикач на все.

---

## 5. Блок D — таймінги (**секундоміром**)

Не косметика. До 2026-09-05 код рахував «тіки», припускаючи ~20/с, а головний цикл
рації крутиться раз на **мілісекунду** — помилка рівно в 50 разів. Тобто таймаут
очікування відповіді був не 8 секунд, а **0,16 с**, і радіоперевірка не могла спрацювати
в принципі. Цей блок перевіряє саме те виправлення, і виміряти його можна тільки
годинником.

| № | Дія | Має бути |
|---|---|---|
| D1 | A5 (неіснуючий ID) із секундоміром | `Немає відповіді` **приблизно через 8 с**. Якщо менше секунди — виправлення не доїхало в прошивку |
| D2 | Після успішної перевірки не чіпай A | Екран результату тримається **~20 с**, потім сам закривається |
| D3 | Надішли SMS (блок F), не чіпай екран результату | Напис `Надіслано` тримається **~30 с** |
| D4 | Надішли SMS і **одразу відпусти зелену** | Екран результату **не закривається** миттєво від тієї ж зеленої (є запобіжник ~0,5 с) |

---

## 6. Блок E — флеш і перезавантаження

Перевіряємо дві різні речі: що налаштування переживають вимикання, і що захист від
повторного відтворення команд не блокує свою ж рацію після перезавантаження. Друге —
реальний дефект, виправлений 2026-09-04; раніше лічильники жили лише в пам'яті.

| № | Дія | Має бути |
|---|---|---|
| E1 | На B виставь дозволи, збережи, **вимкни і ввімкни** B | Дозволи на місці |
| E2 | Перевір B з A (успішно). **Перезавантаж A**. Перевір B ще раз, B не чіпай | Працює. *Раніше тут запити A мовчки ігнорувались* |
| E3 | Перевір B з A. **Перезавантаж B**. Перевір ще раз | Працює |
| E4 | Перезавантаж **обидві**, перевір | Працює |

**E2 — головний крок цього блоку.** Якщо він провалиться, це рівно той дефект, який ми
вважали закритим.

---

## 7. Блок F — SMS: адресний фільтр

Раніше на рацію приходило **все, що є в ефірі**, незалежно від адресата. Тепер є фільтр
і окремий режим монітора.

Меню: **Меню → Повідомлення**. Четвертий пункт — `Монітор: увімк/вимк`.

| № | Дія | Має бути |
|---|---|---|
| F1 | На B: `Монітор` = **вимк**. З A надішли **приватне** повідомлення на **ID B** | Приходить на B, у `Вхідні` |
| F2 | З A надішли приватне на **чужий ID** (не A і не B) | На B **не з'являється нічого** |
| F3 | На B: `Монітор` = **увімк**. Повтори F2 | Тепер з'являється, у списку помічене `>`, при читанні — `(не вам)` |
| F4 | З A надішли на **талкгрупу поточного каналу** | Приходить на B (це «наша» група) |
| F5 | З A надішли на **чужу талкгрупу**, монітор **вимк** | Не приходить. ⚠️ «Чужа» означає ще й **відсутня в списку RX-груп** каналу B — інакше вона для B своя, і повідомлення прийде правильно |
| F6 | Повтори F1 на **шифрованому** каналі | Приходить і читається |

---

## 8. Блок G — тексти на екрані

Дрібниця, але саме так ловляться обрізані рядки: прошивка ріже їх **мовчки**, без
жодного сліду в коді.

| № | Дія | Має бути |
|---|---|---|
| G1 | **Меню → Налаштування → Доступ RCTL** | П'ять пунктів читаються повністю; внизу **два** рядки підказки: `L/R:зміна  GRN:зберегти` і `RED:вийти без збереження` — обидва видно цілком, без обриву |
| G2 | Спробуй надіслати SMS із **зайнятого** каналу (тримай PTT на A і надішли з A) | Напис `Помилка надсилання` видно цілком |
| G3 | Пройди меню з `Розмір тексту` = **2** | S-метр не перекриває написи |
| G4 | Відкрий **Меню → Супутник** | S-метр не лізе на екран супутника |
| G5 | Канал **через ретранслятор**, вийди за межі його дії (або вимкни ретранслятор), натисни **PTT** | `ПОМИЛКА` / `Ретранслятор` / `не відповідає` / `поза зоною дії?` — усе читається, тримається **~3 с**. Раніше тут було слово `TIMEOUT` на 0,5 с |
| G6 | Повтори G5 і одразу натисни будь-яку клавішу | Екран прибирається достроково, рація готова до нового натискання PTT |

---

## 9. Блок H — попапи сповіщень (SQUELCH/VOLUME/POWER/MESSAGE/SMS/BEARING/APO)

Це перевірка `docs/notification-buffer.md` (звільнення CCM + фікс регресії 2026-09-19):
попапи тепер малюються без повноекранного бекапу фону, і саме тому їх варто пройти всі
шість, а не лише той, що зламався першим (SQUELCH).

⚠️ Другу рацію тут використовувати не обов'язково — блок H майже весь про **одну** рацію.
Виняток — H7 (APO + вхідний виклик): туди потрібна друга рація, щоб зателефонувати першій.

| № | Дія | Має бути |
|---|---|---|
| H1 | У **VFO**: клавішами гучності/шумоподавлення (не тримати) зміни рівень шумоподавлення | Картка з'являється **одразу** (не за ~200 мс), на весь екран, з назвою й відсотком; зникає через ~1 с |
| H2 | Те саме в режимі **Каналу** | Той самий результат, що й H1 |
| H3 | Зміни **гучність** (VFO або канал, якщо `HAS_SOFT_VOLUME`) | Картка з'являється одразу, той самий вигляд, що в H1, але з назвою «Гучність» |
| H4 | Зміни **потужність** (SK2 + стрілки) | Картка з'являється одразу, значення на кшталт `500mW`/`1W`/`5W` |
| H5 | **Тримай** клавішу шумоподавлення/потужності кілька секунд поспіль (безперервна зміна) | Картка оновлюється плавно, **без проблиску S-метра чи фону** між кадрами |
| H6 | **Швидко перемкни** шумоподавлення → потужність → шумоподавлення (різні типи поспіль, не чекаючи закриття) | Кожна картка з'являється коректно, попередня коректно змінюється на нову, без «залипання» старого напису |
| H7 | На **зайнятому/заблокованому каналі** (`BIT_CHANNELS_ARE_READ_ONLY`) відкрий деталі каналу, спробуй зберегти зміну | З'являється MESSAGE-картка «Канал APRS / Заблоковано» (невеликий прямокутник по центру, не весь екран) |
| H8 | У деталях каналу зміни **зсув частоти** (shift offset) поза межами діапазону | З'являється MESSAGE-картка з `out_of_band` |
| H9 | Прийди **вхідне SMS** | Банер на весь екран, тримається **без таймауту** (не зникає сам за ~30 с) |
| H10 | Не закриваючи банер SMS, зайди в **будь-яке меню** (наприклад «Меню → Про рацію») | Меню відкривається чисто, **без залишку картки SMS** під ним чи навколо нього |
| H11 | Поверни закрий банер SMS клавішею **RED** | Екран коректно повертається до VFO/каналу, без залишків картки |
| H12 | Якщо є GPS/APRS-контакт із пеленгом (BEARING) — виклич показ пеленгу | Картка (коло+стрілка+відстань) з'являється й зникає через ~2 с, фон під нею коректний |
| H13 | Дочекайся **попередження про автовимкнення (APO)**, приблизно за хвилину до вимкнення | Попап з'являється, тримається до 60 с |
| H14 | **Поки триває H13**, зателефонуй цій рації з іншої (вхідний виклик) | Дані виклику (ID/RSSI) на екрані **оновлюються живими**, попап APO не заважає їх побачити -- саме заради цього залишили старий бекап тільки для APO |
| H15 | Закрий будь-який попап (таймаут або клавішею) і одразу подивись на екран | Екран VFO/каналу відновлюється **повністю й без блимання** -- шапка, S-метр, дані каналу коректні, нічого не «застигло» з картки |

---

## 10. Блок I — «Переслухати» (ENABLE_CALL_REPLAY)

Перевірка потребує прошивки, зібраної з `ENABLE_CALL_REPLAY=1` (у CI це `build-ukrainian`
job — дивись `.github/workflows/build.yml`; звичайна `build`-матриця цей прапорець не
вмикає й пункту меню не матиме). Пункт меню — поруч із «Останні виклики»: **Меню →
Переслухати**. Обидві рації мають бути на **тому самому ВІДКРИТОМУ (не-AES) каналі**,
якщо явно не сказано інше.

⚠️ Друга рація потрібна для всього блоку I, крім I7 (переривання PTT) — туди досить однієї.

| № | Дія | Має бути |
|---|---|---|
| I1 | Заходь у **Переслухати** одразу після холодного старту, буфер ще порожній | MESSAGE-картка «Немає запису», а не тиша й не порожній екран |
| I2 | З іншої рації зроби **1 коротку передачу** (~2-3 с) на відкритому каналі, потім відкрий **Переслухати** і натисни зелену | Чутно записаний голос, від початку передачі, без шуму/спотворень |
| I3 | Зроби **2-3 окремі передачі** (відпускай PTT між ними на 1-2 с) і відтвори | Усі заходи чутні по черзі, **від найстарішого до найновішого**, між ними коротка тиша-пауза (не злипаються в одну репліку, і не грає суцільний шум замість паузи) |
| I4 | Говори **довше ~30 с** без відпускання PTT (або кількома заходами понад 30 с сумарно), потім відтвори | Чутні лише **останні ~30 с** — найстаріший початок обрізаний, останні слова збереглись |
| I5 | Зроби **AES-виклик** (шифрований канал, обидві рації з тим самим ключем) з іншої рації, потім перемкнись на **відкритий** канал і відкрий Переслухати | Записаний AES-виклик відсутній повністю: або буфер порожній (якщо до нього нічого не записувалось), або в записі чутні лише сусідні ВІДКРИТІ передачі, без шуму на місці AES-виклику |
| I6 | Запиши звичайну відкриту передачу, **перемкнись на AES-канал**, і відкрий Переслухати просто на ньому | Відтворення чисте (не шум) — рушій відтворення обходить AES-хук незалежно від того, який канал активний зараз |
| I7 | Почни відтворення (`Переслухати` → зелена), і **натисни PTT** під час програвання | Відтворення обривається негайно, рація готова передавати |
| I8 | Почни відтворення, і **прийми вхідний виклик** з іншої рації під час програвання | Відтворення обривається, показ вхідного виклику працює як завжди |
| I9 | Під час відтворення натисни **RED** | Відтворення зупиняється, меню закривається (повернення на попередній екран) |
| I10 | Переключись у **FM**-режим, попередай на ньому кілька секунд, поверни DMR і відтвори | У записі немає фрагмента з FM (FM взагалі не пишеться) |
| I11 | Зроби **~1-2 передачі**, зайди в **Переслухати**, вийди без відтворення (RED), зайди знову | Буфер не спорожнів — там, де раніше вже записано, лишається доступним; повторний вхід у меню не стирає запис |

---

## 11. Таблиця результатів

Заповни й пришли — далі я працюю вже за нею.

```
A1 __  A2 __  A3 __  A4 __  A5 __
B1 __  B2 __  B3 __
C1 __  C2 __  C3 __  C4 __  C5 __
D1 __ (виміряно: ____ с)   D2 __ (____ с)   D3 __ (____ с)   D4 __
E1 __  E2 __  E3 __  E4 __
F1 __  F2 __  F3 __  F4 __  F5 __  F6 __
G1 __  G2 __  G3 __  G4 __  G5 __  G6 __
H1 __  H2 __  H3 __  H4 __  H5 __  H6 __  H7 __  H8 __  H9 __  H10 __  H11 __  H12 __  H13 __  H14 __  H15 __
I1 __  I2 __  I3 __  I4 __  I5 __  I6 __  I7 __  I8 __  I9 __  I10 __  I11 __
```

## 12. Якщо щось провалилось

Пришли:

1. **Номер кроку** (напр. «C2») — цього вже досить, щоб я знав, куди дивитись.
2. **Фото екрана** обох рацій, якщо на них щось написано.
3. Чи повторюється, чи спрацювало раз через раз. **Нестабільність — це інша причина,
   ніж стабільна відмова**, і шукати її треба в іншому місці.

Не треба нічого діагностувати самому — номер кроку несе більше інформації, ніж здається.

---

## 13. Що цей протокол **не** перевіряє

Щоб не було ілюзії повного покриття:

- **Дальність і поведінку на межі чутності.** Все вище робиться на столі. Як RCTL
  поводиться на слабкому сигналі — окреме питання.
- **Ретранслятор.** Тільки прямий зв'язок (simplex).
- **Мережу більшу за дві рації.** Зокрема залишковий ризик anti-replay проявляється
  лише коли командирських рацій **більше восьми**.
- **Побайтову звірку запису ключів з ПК** — це робиться інакше, не з рації.
