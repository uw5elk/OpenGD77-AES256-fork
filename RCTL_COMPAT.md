# Сумісність RCTL зі стоковим протоколом TYT — план реверс-інжинірингу

**Рішення (2026-09-05, за вказівкою користувача):** «нам потрібна максимальна сумісність
з існуючими протоколами, а не щось окреме». Отже наш власний 16-байтний PDU
(`dmr_rctl_pdu.h`) — **тимчасовий**. Мета — щоб радіоперевірка (і згодом stun/revive)
йшли в ефір **у тому самому форматі, що й стокова TYT**, і щоб стокова рація відповідала
нашій, а наша — стоковій. Рівно так, як уже зроблено зі SMS.

Ціль сумісності — **стокова TYT MD-UV3x0** (Motorola/Hytera користувач не обрав; їх тут
свідомо немає — див. «Межі» нижче).

---

## 0. Вузол, який вирішує все: а чи стокова TYT взагалі це вміє? ✅ ЗАКРИТО

**Відповідь (2026-09-05, фото меню стокової TYT):** так, стокова має **всі чотири**
команди — `Radio Check`, `Remote Mon.`, `Radio Enable`, `Radio Disable`. Це стандартний
MOTOTRBO-подібний набір. Отже є реальний формат в ефірі, який можна захопити й відтворити,
і стокова рація в руках для звірки. Реверс розблоковано — далі розділ 2.

Нижче — оригінальне міркування, лишаю для контексту.

Перш ніж писати бодай рядок коду, треба підтвердити **факт**, а не припущення:

> **Чи є в CPS/меню стокової TYT MD-UV3x0 функція «Radio Check» (і «Radio
> Disable/Stun», «Radio Enable», «Remote Monitor»)?**

Це не риторичне питання. Стокова прошивка TYT доволі проста, і я **не можу стверджувати
з пам'яті**, що радіоперевірка там є. SMS там точно є (ми його й реверснули), а от
командні функції — під питанням.

Три можливі результати:

| Що виявиться | Наслідок |
|---|---|
| Стокова TYT **вміє** Radio Check | ✅ Є що захопити й відтворити. Ідемо розділом 2. |
| Стокова TYT **не вміє** нічого з цього | Немає «існуючого протоколу TYT» для цих команд. Тоді єдиний «існуючий протокол» — стандарт ETSI/DMRA (окрема робота, стокова рація не потрібна). |
| Вміє частину (напр. Radio Check є, Stun немає) | Робимо сумісним те, що є; решту — за стандартом. |

**Дію не можна пропустити.** Якщо збудувати «сумісний» формат наосліп і він не збіжиться
зі стоковим побайтово — стокова рація його просто проігнорує, і ми отримаємо той самий
«Немає відповіді», але вже з ілюзією, що зробили сумісність.

---

## 1. Безкоштовна перша перевірка (робиться вже зараз, без нової прошивки)

У нашій прошивці вже є USB-діагностика, яка рахує **кожен** прийнятий data-burst будь-якого
типу (`dmrSmsRxDiagBurst`, HR-C6000.c). Інструмент — `tools/sms_diag.py`.

1. Одна рація — **наша** (фork), під'єднана USB до ПК, на спільному з тестом каналі.
2. Друга — **стокова TYT**.
3. На ПК:  `python3 tools/sms_diag.py --reset`   (обнулити лічильники)
4. На стоковій TYT — **зробити радіоперевірку** нашої рації (якщо така функція є в меню).
5. На ПК:  `python3 tools/sms_diag.py`   (прочитати лічильники)

Як читати результат (`d`=усі data-burst, `hOk/hBad`=заголовки типу 6, `bOk/bBad`=rate-1/2
типу 7):

| Що показали лічильники | Висновок |
|---|---|
| `d` **не змінився** | Стокова нічого не надіслала в ефір як дані. Або функції немає, або вона йде голосовим/іншим класом. |
| `d` зріс, а `hOk`/`bOk` **ні** | Прийшов burst **не типу 6 і не типу 7** — майже напевно **CSBK (тип 3)**. Саме так найчастіше й переносять radio-check. Це — сигнал, що треба розділ 2. |
| зросли й `hOk`/`bOk` | Стокова шле це як звичайний data-PDU (тип 6+7), як SMS. Тоді захоплення в нас уже майже готове (див. пастку нижче). |

Ця перевірка коштує нуль коду й одразу каже, з чим ми маємо справу.

### ⚠️ Пастка: CSBK ми зараз НЕ дампимо

У HR-C6000.c дамп сирих байтів читається (сторінка 0x02) **лише для типів 6 і 7**:

    if (crcOk && ((rxDataType == 6) || (rxDataType == 7)))
    {
        ... dmrSmsRxBurst / dmrRctlRxBurst ...
    }

CSBK — це **тип 3**. Його `dmrSmsRxDiagBurst` **порахує** (тому `d` зросте), але самі
байти нікуди не потраплять. Тобто якщо стокова шле radio-check як CSBK — а це найімовірніше —
наш поточний захват побачить «щось прийшло», але не покаже, що саме. Це й треба
дописати в кроці 2.1.

---

## 2. План реверсу (той самий метод, що спрацював зі SMS)

Метод SMS задокументований у шапці `tools/dmr_enc_sms.py`: *«Framing CRCs reproduced
byte-exact from off-air captures»* — тобто захопили реальний ефір, розібрали, відтворили
байт-у-байт, звірили на залізі. Повторюємо крок у крок.

### 2.1. Захоплення сирих байтів команди ✅ ЗРОБЛЕНО (2026-09-05)

Готово й перевірено:
* `dmr_rctl_cap.c/.h` — кільце на 8 сирих burst у CCM (тип + 12 інфо-байтів), armed лише
  з ПК, "стоп-коли-повне" береже початок послідовності. Хостовий тест `test_dmr_rctl_cap.c`
  (7/7 зелено).
* хук у `HR-C6000.c`: коли захоплення ввімкнено, сторінка 0x02 читається для **будь-якого**
  типу data-sync, зокрема **CSBK (тип 3)**, а не лише 6/7. У звичайній роботі вимкнено —
  зайвого SPI-читання в ISR немає.
* USB-команди `0x98` (arm/disarm), `0x99` (dump), `0x9A` (reset) у `usb_com.c`.
* інструмент `tools/rctl_capture.py`.

**Сеанс захоплення (на робочому місці, дві рації):**

1. Форк-рація (RX, по USB до ПК) і стокова TYT на **спільному** каналі (частота/TS/CC/TG),
   мінімальна потужність, канал тихий.
2. `python3 tools/rctl_capture.py --arm`
3. На **стоковій** зробити **одну** дію — напр. `Radio Check` на DMR ID форк-рації.
4. `python3 tools/rctl_capture.py` — надрукує захоплені burst у hex.
5. Повторити для `Remote Mon.`, `Radio Enable`, `Radio Disable` (щоразу `--arm` перед дією).
6. Надіслати роздруківки — за ними робимо розбір (2.2).

Якщо нічого не захопилось — спершу `sms_diag.py` (розділ 1): чи `d` узагалі росте.

### 2.2. Розбір формату ✅ ЗРОБЛЕНО (2026-09-05)

Захоплено всі чотири команди зі стокової TYT (tools/rctl_capture.py), CRC перевірено
математично на кожному кадрі. **Формат — стандартний Motorola CSBK (тип 3), ВІДКРИТИМ
ТЕКСТОМ (жодного AES).**

Кадр команди — 12 байт:

    [b0] [FID=0x10] [0x00] [cmd] [src:3] [dst:3] [crc:2]

| Команда | b0 | опкод CSBK | cmd (b3) |
|---|---|---|---|
| Radio Check    | 0xA4 | 0x24 | 0x00 |
| Remote Monitor | 0x9D | 0x1D | 0x01 |
| Radio Enable   | 0xA4 | 0x24 | 0x7E |
| Radio Disable  | 0xA4 | 0x24 | 0x7F |

* Check / Enable / Disable — той самий опкод 0x24, різняться керуючим байтом (Enable/Disable
  = 0x7E/0x7F, різниця в один біт, як Inhibit/Uninhibit у Motorola). Monitor — окремий
  опкод 0x1D.
* FID = 0x10 (Motorola/DMRA) на всіх.
* Порядок адрес у команді — **src, потім dst** (у преамбулі навпаки: dst, src).
* CRC — CRC-CCITT "d" (poly 0x1021, init 0, xor 0xFFFF) над 10 байтами ^ 0xA5A5. Той самий,
  що ми вже маємо для преамбул SMS.

Перед командою — послідовність преамбул CSBK, що рахують кадри вниз від ~16:

    [0xBD] [0x00] [0x00] [лічильник] [dst:3] [src:3] [crc:2]

> ℹ️ **Про ID у захватах.** Ціль `2550287` (0x26EA0F) — це ID, який набрали на стоковій під
> час зняття; **справжній ID форк-рації — 2550313** (0x26EA69). На формат це не впливає
> (змінюються лише 3 байти адреси + CRC, які білдер рахує для будь-якого ID), тож захвати
> лишаються валідним провенансом. Для двонапрямної валідації (крок 2.5) цілитись треба на
> 2550313.

**Сирі захвати (стокова 2550333 -> набрана ціль 2550287), для провенансу:**

    check:   a4 10 00 00 26 ea 3d 26 ea 0f 93 7c
    monitor: 9d 10 00 01 26 ea 3d 26 ea 0f a3 88
    enable:  a4 10 00 7e 26 ea 3d 26 ea 0f 25 95
    disable: a4 10 00 7f 26 ea 3d 26 ea 0f 9d f4

### 2.3. Відтворення TX ✅ РЕФЕРЕНС ГОТОВИЙ (2026-09-05)

`tools/rctl_stock.py` будує всі чотири команди й обидва типи преамбул. Self-test у ньому
відтворює реальні захвати **байт-у-байт** (усі 4 команди + 3 преамбули). Це python-референс;
наступний крок — портувати цей формат у прошивку (2.4).

### 2.4. Заміна, а не паралель

Прибрати `dmr_rctl_pdu.h`-формат з ефіру й пустити стоковий. **Модель дозволів
(`Доступ/Перевірка/Моніторинг/Вимкнення/Ввімкнення`) лишається** — вона керує тим, що
рація погоджується **виконати**, і не залежить від формату на дроті. Anti-replay теж
лишається там, де формат це дозволяє.

### 2.5. Валідація на залізі

**✅ ПІДТВЕРДЖЕНО В ЕФІРІ (2026-09-05): форк -> стокова, команда Monitor.**
Форк надіслав Remote Monitor на стокову (2550333) стоковим форматом. Стокова **прийняла
команду й сама вийшла в ефір** приватним голосовим викликом на форк -- захоплено Voice-LC
header:

    03 00 00 26 ea 29 26 ea 3d ...   (FLCO 0x03 приватний; dst=2550313 форк; src=2550333 стокова)

Це доводить, що наш TX байт-правильний і заводська рація виконує наші команди. Radio Check /
Enable / Disable ідуть тим самим TX-шляхом.

**Ще не зроблено:**
* ACK Radio Check від стокової форк не встиг зловити (таймінг: миттєвий CSBK одразу після
  нашого TX, поки форк ще в teardown -- на відміну від Monitor, де голос триває секунди).
  Потрібен для екрана "рація на зв'язку" і для відповіді форку як цілі.
* RX-бік: форк як ЦІЛЬ (розпізнати вхідну стокову команду -> виконати: Check=ACK,
  Monitor=відкрити мік, Enable/Disable=локнутись/розлочитись), під дозволами.

---

## 3. Межі (свідомо)

- **Motorola / Hytera:** користувач їх не обрав, і реального обладнання для звірки немає.
  Будувати «наосліп» за напівзгаданими опкодами я не буду — це рівно той клас
  припущень, який уже двічі коштував цьому проєкту дефектів (частота тіків, UTF-8).
- **Універсальний stun/monitor чужих комерційних рацій** лишається за межею, яку ми
  провели в PLANS.md (розділ 3): сумісність «тільки власний флот» — так, інструмент проти
  третіх осіб — ні. Стокова TYT у власному флоті — це саме дозволений випадок.
- Поки не пройдено розділ 0, оцінки обсягу немає: усе залежить від того, що реально
  вміє стокова рація.

---

## 4. Що зробити першим (порядок)

1. ~~Перевірити наявність команд на стоковій.~~ ✅ є всі чотири (фото).
2. ~~Дописати захоплення сирих байтів.~~ ✅ зроблено (крок 2.1).
3. ~~Захопити 4 команди зі стокової.~~ ✅ зроблено (2026-09-05).
4. ~~Розбір формату (2.2) + референс TX (2.3).~~ ✅ зроблено, CRC і байти перевірено.
5. ~~Портувати стоковий формат у прошивку замість нашого PDU (2.4) — TX цих CSBK і RX.~~
   ✅ зроблено: TX усіх 4 команд, RX із гейтом дозволів, стокова Disable/Enable → сон/
   пробудження форка (повний stun: екран/звук/клавіші мертві, приймач живий), кабельний
   і ефірний вихід. Дозволи RCTL відновлюються з ПК (`rctl_capture.py --rctl-on`).
6. **Разом:** валідація стокова ↔ наша, обидва напрямки (2.5). ✅ Monitor і Disable/Enable
   підтверджено на залізі.

---

## 5. ВІДКЛАДЕНО: квитанція (ACK) — «онлайн/успішно» в командира

**Стан:** сон/пробудження/дозволи повністю робочі. Бракує лише щоб командир (стокова/
RT4D) бачив нашу рацію як «онлайн» після Radio Check і «успішно» після Disable/Enable —
для цього ціль має слати назад квитанцію-CSBK.

**Чому відкладено (2 бар'єри):**
1. *Формату не маємо.* Пасивно зловити квитанцію стокової з форка НЕ ВИЙШЛО, і це
   вичерпано: захоплювач уже ловить кожен data-sync burst із будь-якого слота й без
   перевірки CRC (HR-C6000.c, блок за `transmissionEnabled==false`), а відповідь у дампі
   не з'являється. Причина — на рівні радіочіпа: після завершення передачі командира чіп
   заново шукає синхронізацію й не встигає на єдиний короткий burst-квитанцію (без власної
   преамбули) у проміжку. Безпечної програмної зміни, що це полагодить, немає.
2. *Навіть з форматом — TX має бути слот-точним.* Квитанція мусить лягти у конкретний
   TDMA-слот одразу після команди; наш шлях «ISR→tick→dmrDataTxLoad» до цього не
   прив'язаний. Це окрема робота в TX-тракті.

**План, коли повернемось (є обладнання):** зняти формат квитанції з ефіру через
**HackRF + PortaPack** (ловить безперервно, без «сліпої зони» форка) або RTL-SDR +
DSD-FME/SDRTrunk (друкують CSBK у hex). Сценарій: RT4D/стокова робить Radio Check на
стокову (ціль), SDR ловить увесь обмін → маємо точні байти квитанції для Check; так само
для Disable/Enable. Далі — реалізувати слот-точний TX відповіді.

**Довідка (захоплено 2026-09-05):** RT4D (ID 2550299) → стокова (2550333), Radio Check:
преамбули `bd 00 00 NN 26ea3d 26ea1b crc` (відлік NN→1, dst,src), потім дві команди
`a4 10 00 00 …` (cmd=00) і `a4 10 00 80 …` (cmd=80), src=RT4D dst=стокова. Квитанції
стокової в захопленні НЕ було (див. вище).

---

## 5a. КВИТАНЦІЮ ЗНЯТО З ЕФІРУ (2026-09-11) -- бар'єр "формату не маємо" ЗНЯТО ✅

Формат квитанції Radio Check захоплено HackRF Portapack і декодовано власним
декодером (`tools/dmr_csbk_rx.py`), CRC валідний, відтворено у двох незалежних
записах.

**Квитанція стокової TYT (2550333) у відповідь на Radio Check від RT4D (2550299):**

    a4 10 00 80 26 ea 1b 26 ea 3d 3e 43
    |__| |  |__| |______| |______| |___|
     |   |   |       |        |      CRC
     |   |   |       |        +-- 2550333 = ХТО відповідає (стокова)
     |   |   |       +-- 2550299 = КОМУ (той, хто питав)
     |   |   +-- керуючі байти: 00 80  (у команді було 00 00)
     |   +-- FID 0x10 (Motorola/DMRA)
     +-- опкод 0xA4 (CSBKO 0x24 + LB) -- ТОЙ САМИЙ, що в команді

**Правило формування квитанції (просте):** взяти отриману команду, виставити
**старший біт керуючого байта** (0x00 -> 0x80), адресні поля лишити БЕЗ ЗМІН
(вони просто віддзеркалюються), перерахувати CRC. Стокова повторює квитанцію
~3 рази (хвіст в ефірі триває 82 мс ~= 3 burst-и).

**Як доведено, що це саме квитанція стокової, а не друга команда RT4D:**
1. *Вимкнення цілі.* Зі стоковою ВИМКНЕНОЮ RT4D повторює запит тричі (20 burst-ів),
   і 82-мс хвоста немає ЖОДНОГО. З увімкненою -- хвіст є завжди, рівно один,
   одразу після команди.
2. *Амплітуда.* Коли приймач стояв біля RT4D, хвіст був слабшим за команди
   (0.086 проти 0.100); коли біля стокової -- у 2.4 раза сильнішим (0.212 проти
   0.090). Співвідношення перевернулось разом із приймачем -> різні передавачі.
3. Байт 0x80 -- класичний прапорець відповіді (старший біт).

Раніше в цьому файлі значилось "дві команди a4 10 00 00 і a4 10 00 80, обидві
src=RT4D" -- **це було хибне припущення**: друга з них насправді і є квитанція
стокової. Адресні поля збігаються, бо квитанція їх віддзеркалює.

**Що лишилось для #10:** другий бар'єр -- слот-точний TX відповіді
(шлях ISR->tick->dmrDataTxLoad до TDMA-слота не прив'язаний). Формат більше не
блокує.

**Параметри декодування ефіру (здобуті емпірично, звірені по CRC) -- у
`tools/dmr_csbk_rx.py`:** мапінг ETSI (рівні -3,-1,+1,+3 -> дибіти 11,10,00,01);
burst = 98 info|10 slot-type|48 sync|10 slot-type|98 info; BPTC deinterleave
d[i]=raw[(i*181)%196], матриця 13x15 з індексу 1 рядково, інфо = рядки 0..8 x
стовпці 0..10 зі зсувом 3; CSBK CRC = crc16-CCITT(0x1021, init 0) ^ 0x5A5A, BE.

**Урок про захоплення ефіру:** КЛІП убиває дані остаточно, слабкий сигнал -- ні.
Перший придатний запис мав пік |IQ| 0.10 і декодувався; записи в насиченні
(пік 1.41) не врятувала навіть корекція помилок BPTC. Тримати пік 0.2..0.8;
якщо рація поруч -- зняти антену з Portapack і гейни в мінімум.
