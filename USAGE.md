# TYT MD-UV390UKR — посібник користувача

Українська прошивка для радіостанцій **TYT MD-UV380 / MD-UV390** (та сумісних
Retevis RT-3S), побудована на відкритому проєкті **OpenGD77** Роджера Кларка VK3KYY
і його учасників.

Документ складається з двох частин:

- **Частина I — для оператора.** Як користуватись рацією, на якій уже стоїть ця
  прошивка. Читати з нуля, навіть якщо ви ніколи не тримали DMR-рацію.
- **Частина II — для техніка.** Як прошити рацію, записати ключі шифрування,
  налаштувати канали й дистанційне керування для цілого парку рацій.

> ⚠️ **Юридичне застереження.** Шифрування голосу й повідомлень, яке є в цій
> прошивці, **заборонене на аматорських діапазонах у більшості країн**, зокрема в
> Україні. Ці функції призначені для ліцензованого службового/PMR використання.
> Відповідальність за дотримання правил зв'язку несе власник рації.

---

# Частина I — для оператора

## 1. Що це за прошивка і чим вона відрізняється

Це OpenGD77 з українською локалізацією та кількома доданими функціями:

| Що додано | Коротко |
|---|---|
| **Українське меню** | Весь інтерфейс українською, з правильними Ї та І |
| **Шифрування голосу AES-256** | Сумісне зі стоковим TYT «Universal AES256» |
| **Шифровані SMS** | Текстові повідомлення між рацією через DMR |
| **Дистанційне керування (RCTL)** | Наразі одна команда — «чи на зв'язку рація» |
| **S-метр** | Смуга рівня сигналу зі шкалою і значенням у дБм |
| **Гасла** | «СЛАВА УКРАЇНІ!» при вмиканні, «ГЕРОЯМ СЛАВА!» при вимиканні |
| **Яскравіші теми** | Денна й нічна теми з насиченішими кольорами |

Усе інше — канали, зони, сканування, GPS, APRS — працює так само, як у звичайному
OpenGD77, тож офіційний посібник OpenGD77 теж застосовний.

## 2. Вмикання і вимикання

Живлення вмикається **ручкою гучності** зверху (вона ж регулятор гучності).

- При вмиканні на екрані з'явиться **СЛАВА УКРАЇНІ!**
- При вимиканні — вікно **Вимкнення...** і **ГЕРОЯМ СЛАВА!**

Якщо на екрані вмикання ви бачите щось інше (картинку або текст) — у кодплаг
записано власну заставку; це нормально, зверніться до техніка.

**Комбінації при вмиканні** (тримати кнопку, вмикаючи рацію):

| Комбінація | Що робить |
|---|---|
| **SK2** | Скидання налаштувань до заводських |
| **SK2 + ▼** | Стерти власну заставку й мелодію |
| **SK1 + 0** | Безпечний запуск (safe boot) |
| **#** | Увімкнути голосові підказки |

## 3. Органи керування

На MD-UV380/390 є: **дві бічні кнопки** (у прошивці вони називаються **SK1** і
**SK2**), **тангента PTT**, **цифрова клавіатура**, дві клавіші **▲/▼** під
екраном, **зелена** і **червона** клавіші, **ручка каналів** (поворотний енкодер)
і **ручка гучності** (вона ж вимикач).

> **Оранжевої кнопки на цій моделі немає.** У багатьох інструкціях до OpenGD77
> згадується оранжева кнопка — на MD-UV380/390 її роль виконує комбінація
> **SK1 + зелена**.

### 3.1. Головні дії на екрані каналу

| Кнопка | Дія |
|---|---|
| **Ручка каналів** | Наступний / попередній канал |
| **SK2 + ручка** | Наступна / попередня **зона** |
| **▲ / ▼** | DMR: наступна/попередня талкгрупа. FM: шумодав +/− |
| **SK2 + ▲ / ▼** | Потужність більше / менше |
| **SK2 + ▲ довго** | Одразу максимальна потужність |
| **▲ довго** | Почати сканування зони |
| **Зелена** | Головне меню |
| **Червона** | Перейти в режим **VFO** |
| **SK1 + зелена** | Швидке меню каналу |
| **SK2 + зелена** | Деталі каналу |
| **SK1 (тримати)** | Показати деталі каналу, поки тримаєте |
| **SK2 (тримати довго)** | **Монітор** — відкрити шумодав, зняти фільтри |
| **SK2 коротко** | Прийняти TG/таймслот/колір-код того, хто зараз говорить |
| **SK1 + червона** | Перемкнути тему день/ніч |
| **SK1 + червона довго** | Повернути автоматичне перемикання теми |
| **SK2 + червона** | Вийти з приватного виклику, повернути талкгрупу |
| **\*** | DMR: перемкнути таймслот TS1/TS2. FM: смуга 12.5/25 кГц |
| **SK2 + \*** | Перемкнути канал DMR ↔ FM |
| **\* довго** | Скинути ручне перемикання таймслоту |
| **#** | Ввести номер талкгрупи/ID з клавіатури |
| **SK2 + #** | Швидкий список контактів |
| **# довго** | Реверс ретранслятора (поміняти RX і TX місцями) |
| **Цифри 0–9** | Набрати номер каналу, підтвердити зеленою |
| **0 довго** | Вимкнути/увімкнути звук (Mute) |
| **8 довго** | Показати азимут і відстань до каналу (якщо задані координати) |
| **PTT** | Передача |

### 3.2. Відмінності в режимі VFO

| Кнопка | Дія |
|---|---|
| **Червона** | Повернутись у режим каналу |
| **Червона довго** | Перемкнути VFO A ↔ VFO B |
| **Цифри 0–9** | Ввести частоту (8 цифр) |
| **Ручка** | Крок частоти вгору/вниз |
| **SK1 + SK2 (тримати)** | Реверс ретранслятора |
| **# довго** | Панорамне сканування (спектр) |
| **▼ довго** | Вийти з режиму сканування |

### 3.3. У меню

| Кнопка | Дія |
|---|---|
| **▲ / ▼**, **ручка** | Рух по списку |
| **Зелена** | Вибрати / зберегти |
| **Червона** | На рівень назад |
| **Червона довго** | Вийти одразу на головний екран |
| **\*** | Заблокувати клавіатуру (розблокувати: **SK2 + \***) |
| **#** | Заблокувати PTT |
| **SK2 + цифра** | Призначити цей пункт на швидку клавішу |

Пізніше цю швидку клавішу викликають **SK2 + та сама цифра** з головного екрана,
а стирають — **SK2 + та сама цифра, довго**.

## 4. Що показує головний екран

```
┌────────────────────────────────────┐
│ DMR  TS1  500mW  C1          07:47 │  ← шапка
│                                    │
│             PARROT                 │  ← хто говорить / талкгрупа
│ ┌────────────────────────────────┐ │
│ │████████████████                │ │  ← S-метр (якщо увімкнено)
│ └────────────────────────────────┘ │
│   1     3     5     7     9   -101 │  ← шкала і рівень у дБм
│         Приватний виклик           │  ← назва каналу / тип виклику
└────────────────────────────────────┘
```

**Шапка:** режим (DMR/FM), таймслот (TS1/TS2), потужність, колір-код (C1),
праворуч — час або напруга акумулятора.

**S-метр** (якщо увімкнений — див. п. 5): смуга росте зліва направо, зелена до S9,
далі червона. Цифри під смугою — позначки S1, S3, S5, S7, S9. Праворуч — точне
значення в дБм (від'ємне число; що менше за модулем, то сильніший сигнал: −85
набагато краще, ніж −114).

**Іконка замка** праворуч під шапкою означає, що **канал налаштований на
шифровану передачу**. Уважно: вона показує саме налаштування каналу, а **не**
факт, що поточний виклик справді шифрується — див. розділ 8.

## 5. Увімкнути S-метр

За замовчуванням S-метр **вимкнено**, у шапці показується звичайна тонка смуга.
Щоб увімкнути:

**Меню → Налаштування → Дисплей → S-метр → On** (перемикається клавішами ◄ ►).

Там же поруч корисне: **Яскравість**, **Нічна яскр.**, **Авто ніч** (автоматичне
перемикання теми), **Час**, **Батарея** (V або %), **Розмір тексту**.

## 6. Канали, зони, талкгрупи

- **Канал** — готова комбінація частоти, таймслота, колір-коду й талкгрупи.
  Перемикається **ручкою каналів**.
- **Зона** — група каналів (наприклад, «Місто», «Виїзд»). Перемикається
  **SK2 + ручка** або через **Меню → Зони**.
- **Талкгрупа (TG)** — «канал розмови» всередині DMR. Змінюється **▲/▼** або
  вводиться вручну через **#**.

Щоб швидко відповісти тому, кого щойно почули, натисніть **SK2 коротко** — рація
підставить його талкгрупу, таймслот і колір-код.

## 7. Передача

1. Оберіть канал.
2. Перевірте потужність у шапці (змінюється **SK2 + ▲/▼**).
3. Натисніть і тримайте **PTT**, зачекайте пів секунди, говоріть.
4. Відпустіть PTT.

**Не передавайте без антени** — це псує підсилювач потужності.

## 8. Шифрування голосу (AES)

### Як це працює

У рації є **15 слотів** для ключів AES-256. Ключ — це 64 шістнадцяткові символи
(0–9, A–F). Шифрування вмикається **окремо для кожного каналу**.

**Прийом розшифровується автоматично**, вмикати нічого не треба — якщо потрібний
ключ є у ваших слотах і канал це дозволяє.

### Що зробити оператору

Зазвичай ключі записує технік. Якщо треба вручну з рації:

1. **Меню → Налаштування → Ключі AES**
2. Оберіть рядок `Key 1`…`Key 15` (проти зайнятих стоїть `set`, проти порожніх `---`),
   натисніть **зелену**.
3. Введіть 64 символи:
   - **0–9** — цифри з клавіатури;
   - **▲/▼** — прокрутити символ до **A–F** (літери інакше не ввести);
   - **◄ ►** — рух курсора;
   - **зелена** — зберегти, **червона** — скасувати.
4. Щоб стерти ключ: на рядку `Key N` натиснути **SK2 + зелена**.

> Екран «Ключі AES» усередині поки що англійською — назви `Key N`, `set`, `---`,
> `Global TX key` не перекладені.

### Як увімкнути шифрування на каналі

**Меню → Канал → Шифр. TX** — і оберіть клавішами ◄ ►:

| Значення | Що означає |
|---|---|
| `Off` | Цей канал завжди передає **відкрито** |
| `Key 1`…`Key 15` | Цей канал шифрує **вказаним слотом** |
| `Inherit` | Використати загальний ключ (`Global TX key`) |

**Рекомендований порядок:** поставити `Global TX key` = `Off`, а шифрування
вмикати окремо на кожному потрібному каналі. Інакше загальний ключ увімкне
шифрування **на всіх цифрових каналах одразу**, включно з тими, де воно не
потрібне або незаконне.

### ⚠️ Що обов'язково знати про шифрування

- **Якщо слот порожній, рація передасть ВІДКРИТО і нічого не скаже.** Іконка
  замка при цьому все одно горітиме. Завжди перевіряйте, що проти потрібного
  слота стоїть `set`.
- **Іконка замка = налаштування каналу, а не стан ефіру.** Вона не означає, що
  саме зараз щось шифрується.
- **Немає індикації, що вхідний виклик розшифровується.** Якщо чуєте голос —
  ключ підійшов.
- Якщо ключа немає, рація коротко покаже **«Немає ключа N»** або **«Інший ключ: N»** —
  так пояснюється тиша замість голосу.
- Прийом теж обмежується налаштуванням каналу: якщо на каналі стоїть `Key 3`,
  виклик, зашифрований іншим ключем, **не розшифрується**, навіть якщо той ключ
  у вас записаний.

## 9. Повідомлення (шифровані SMS)

### Надіслати

1. **Головне меню → Повідомлення → Нове повідомлення**
2. Наберіть текст із клавіатури (як SMS на кнопковому телефоні — кілька натискань
   на цифру). **◄** стирає символ. Максимум — зазвичай 144 символи, лічильник
   видно на екрані.
3. **Зелена** → екран «Одержувач».
4. Тут: наберіть **DMR ID** цифрами, або натисніть **SK1** і виберіть зі списку
   контактів. **▲/▼** перемикає **Тип: Група / Приватний**.
5. **Зелена** — надіслати. З'явиться `Надіслано` або `Не надіслано` з причиною.

### Читати

**Головне меню → Повідомлення → Вхідні.** Зірочка на початку рядка означає
непрочитане. **Зелена** — відкрити, ще раз **зелена** — відповісти.
**SK2 + зелена** — видалити. **SK2 + червона** — позначити всі прочитаними.

Про нове повідомлення рація сповіщає одразу: спливаюче вікно, звуковий сигнал і
**іконка конверта** з кількістю непрочитаних на головному екрані.

### Монітор (прослуховування чужих повідомлень)

**Головне меню → Повідомлення → `Монітор: вимк/увімк`**, перемикає **зелена**.

За замовчуванням **вимкнено**: рація приймає лише те, що адресоване саме їй —
приватні на її DMR ID, групові на TG активного каналу, на будь-який TG зі списку
RX-груп цього каналу, і All Call. Решта відкидається мовчки, ще до розшифровки:
без запису в пам'ять, без банера, без звуку.

Коли **увімкнено** — рація приймає все, що змогла відкрити своїм ключем, зокрема
призначене іншим. Такі повідомлення видно окремо:

- у списку «Вхідні» — символ **`>`** перед номером відправника;
- у відкритому повідомленні — напис **`(не вам)`** у рядку «Від»;
- у спливаючому вікні — **`SMS>:`** замість `SMS:`.

Вмикайте монітор свідомо. У ньому чужий трафік займає ту саму спільну пам'ять
і витісняє ваші власні повідомлення, а кожне прийняте — це запис у флеш, банер
і звук.

### ⚠️ Що знати про SMS

- Повідомлення шифруються **тим самим ключем**, що й голос на цьому каналі.
- **Якщо ключ не підібрався, повідомлення піде відкритим текстом** — і рація все
  одно напише `Надіслано`. Попередження не буде.
- **Якщо в одержувача немає ключа, повідомлення просто зникне** — у нього не
  буде ні тексту, ні сповіщення про помилку.
- **Підтвердження доставки немає.** `Надіслано` означає лише «вийшло в ефір».
- **Шифрування ≠ адресність.** Фільтр відсікає чуже за адресатом, але всі, хто має
  ваш ключ і кому повідомлення адресоване, читають його однаково. Якщо на весь
  підрозділ один ключ, «приватне» повідомлення приватне лише за адресою в
  заголовку — сам ключ тут нічого не розділяє.
- Пам'ять спільна для «Вхідних» і «Надісланих» — приблизно 1,3 КБ (це близько
  8 повідомлень по 144 символи або ~28 коротких). Коли закінчується — **найстаріше
  видаляється автоматично**.

## 10. Дистанційне керування (Radio Check)

Дозволяє перевірити, чи інша рація вашого підрозділу увімкнена й на зв'язку, **не
турбуючи її оператора**.

### Дозволити керування своєю рацією

**Меню → Налаштування → Доступ RCTL** → **◄ ►** перемикає `Стан: On/Off` →
**зелена** зберігає. Червона виходить **без збереження**.

За замовчуванням — **вимкнено**.

### Перевірити іншу рацію

1. **Головне меню → Від. керування**
2. Наберіть **DMR ID** цифрами (**◄** стирає) або натисніть **SK1** і оберіть
   контакт зі списку.
3. **Зелена** — надіслати. Рація чекає близько **8 секунд**.
4. Результат: **«Рація на зв'язку»** з ID і часом, або **«Немає відповіді»**.

### ⚠️ Що знати про RCTL

- **Треба увімкнути «Доступ RCTL» на ОБОХ раціях** — і на тій, що питає, і на тій,
  що відповідає. Інакше буде «Немає відповіді».
- **Працює і на відкритих, і на шифрованих каналах.** Якщо на каналі є ключ — команда
  йде зашифрованою; якщо ключа немає — відкритим кадром. Раніше без ключа віддалене
  керування не працювало взагалі.
- ⚠️ **На відкритому каналі команду може надіслати будь-хто в зоні чутності.** Там немає
  ключа, отже немає й підтвердження, що команда від свого. Захист від повторного
  відтворення тут не рятує: чужу команду можна не записувати, а просто скласти. На
  шифрованому каналі таку команду відсіє відсутність ключа.
- **Оператор перевіреної рації нічого не бачить і не чує.** Перевірка повністю
  прихована.
- **Модель довіри проста:** увімкнено — може перевірити будь-хто, хто має ваш
  канальний ключ; вимкнено — ніхто. Списку дозволених ID немає (так само роблять
  Motorola й Hytera).
- Працює **тільки між раціями на цій прошивці**.
- Наразі реалізована **одна команда** — Radio Check. Віддалене прослуховування й
  блокування рації заплановані, але **не працюють**.
- ⚠️ **Функція не перевірена на реальних раціях.** Перед польовим використанням
  обов'язково перевірте на двох рухомих станціях у безпечних умовах.
- **Захист від повторного відтворення переживає вимкнення** (виправлено 2026-09-04).
  Раніше він жив лише в пам'яті й обнулявся при кожному ввімкненні, тож записаний з
  ефіру запит спрацьовував знову після кожного перезавантаження. Тепер лічильники
  зберігаються у флеші.
- ⚠️ **Залишковий ризик.** Рація пам'ятає лічильники лише для **8 останніх** командирських
  рацій. Якщо в мережі їх більше, найстаріші записи витісняються, і дуже старий записаний
  запит від витісненої рації може пройти ще раз. Практичний висновок той самий: тримайте
  «Доступ RCTL» вимкненим, доки він реально не потрібен, і не лишайте його увімкненим
  там, де вихід в ефір небезпечний.

## 11. Часті проблеми

| Симптом | Що перевірити |
|---|---|
| Чути шум, але не чути голос | Ймовірно шифрований виклик без ключа — рація покаже «Немає ключа» |
| Нас не чують | Потужність, антена, правильна талкгрупа й таймслот, колір-код |
| Нічого не чути на каналі | Натисніть і потримайте **SK2** (монітор) — чи є взагалі сигнал |
| Клавіатура не реагує | Можливо заблокована: **SK2 + \*** |
| Екран не читається на сонці/вночі | **SK1 + червона** — перемкнути тему день/ніч |
| Немає S-метра | Меню → Налаштування → Дисплей → S-метр → On |
| Не туди пішла передача | Перевірте зону і канал; **SK2 + червона** виходить з приватного виклику |

---

# Частина II — для техніка

## 12. Що потрібно

| Річ | Навіщо |
|---|---|
| `TYT MD-UV390UKR.exe` | Прошивальник (Windows, нічого ставити не треба) |
| `MD9600-CSV(2571V5)-V26.45.bin` | **Донор кодека AMBE** — без нього DMR не працюватиме |
| Кабель програмування | Звичайний USB-кабель для TYT |
| Zadig | Один раз на комп'ютер — драйвер для режиму DFU |
| CHIRP | Кодплаг: канали, зони, контакти |

Прошивальник збирається автоматично: **GitHub → Actions → останній зелений запуск
→ Artifacts → `openuv380-flasher-windows`**.

Донор кодека в .exe **не вбудований** — його не можна розповсюджувати. Перевірити
кандидата можна командою `python3 tools/verify_codec_donor.py <файл.bin>`.

## 13. Прошивка

Детально — у `BUILD.md`. Стисло:

1. Увімкнути рацію **в режимі DFU**: тримати **SK1 + PTT** і ввімкнути живлення.
   (Якщо на рації вже стоїть OpenGD77 з підтримкою DMR-даних, програма переведе її
   в DFU сама — нічого тримати не треба.)
2. Один раз на комп'ютер: **Zadig** → прив'язати драйвер **WinUSB** до пристрою
   `0483:df11`. Важливо: запускати Zadig **тоді, коли рація вже в DFU**.
3. Запустити `TYT MD-UV390UKR.exe`, натиснути **«Обрати файл донора...»**, вказати
   MD9600, потім **«ПРОШИТИ»**.

**Прошивати можна і зі стокової прошивки TYT** — наявність OpenGD77 не потрібна.
Перед першим переходом збережіть оригінальну прошивку й кодплаг.

## 14. Запис AES-ключів

Три робочі способи:

**А. З комп'ютера (найзручніше для парку рацій)**
Прошивальник → **«Керування AES-ключами...»**. Рація має бути **увімкнена у
звичайному режимі** (не в DFU). Далі: номер слота 1–15, поле на 64 hex, кнопки
«Записати ключ» / «Очистити ключ», окремо «TX-ключ» і «Оновити список зайнятих
слотів».

**Б. Через CHIRP.** Ключі лежать у стоковій таблиці TYT, тож звичайне
читання/запис кодплагу переносить їх разом з усім іншим.

**В. З рації вручну** — див. розділ 8.

> ⚠️ Скрипт `tools/aes_key_store.py --key` **більше не працює** (пише в блок, який
> прошивка не читає). Робочі: `aes_key_tool.py --key`, GUI, CHIRP.

> ⚠️ Шлях запису ключів з ПК **не звірявся побайтово з реальною рацією**. Разово
> перевірте: записати ключ з програми → відкрити на рації «Ключі AES» → має
> з'явитись `set`.

## 15. Налаштування каналів під шифрування

Порядок для парку рацій:

1. Записати однакові ключі в однакові слоти на всіх раціях підрозділу.
2. На **кожній** рації поставити `Global TX key` = **Off**.
3. На кожному каналі, який має бути шифрованим, виставити **Шифр. TX** = потрібний
   слот. Решту каналів лишити `Off`.

Так не буде ситуації, коли рація несподівано шифрує там, де не треба.

Пункт «Шифр. TX» доступний лише на **цифрових** каналах; на аналогових і у VFO
показує `Н/Д`.

## 16. Дистанційне керування (RCTL)

- Увімкнути приймання: на рації **Налаштування → Доступ RCTL**, або з комп'ютера —
  прошивальник → **«Віддалене керування (RCTL)»**, або `tools/rctl_config.py --enable`.
- Обидва способи пишуть той самий блок налаштувань, тож їх можна змішувати.
- **Списку дозволених ID немає** — модель довіри бінарна: увімкнено/вимкнено.
  Реальна межа довіри — **AES-ключ каналу**.
- Для роботи RCTL **обов'язково потрібен AES-ключ** на каналі.

**Відомий нюанс:** захист від повтору команд рахує послідовність від моменту
ввімкнення рації-відправника. Якщо перезавантажити рацію, яка надсилає запити, а
цільову лишити ввімкненою, її запити якийсь час можуть **мовчки ігноруватись**.
Обхід — перезавантажити цільову рацію.

## 17. Кодплаг

Канали, зони, контакти, назви — усе через **CHIRP** (модуль OpenGD77), звичайне
читання/запис. Рація має бути увімкнена у звичайному режимі.

Заставка при вмиканні: якщо в кодплаг записано власну картинку, вона показується
**замість** гасла «СЛАВА УКРАЇНІ!».

## 18. Що ще не перевірене на залізі

Це важливо знати перед тим, як роздавати рації:

| Функція | Стан |
|---|---|
| Прошивка, DMR, голос | Перевірено на MD-UV390 10W Plus |
| AES-шифрування голосу | Перевірено на залізі |
| Запис ключів з ПК | Криптографія й формат перевірені офлайн; побайтова звірка з рацією — **ні** |
| Шифровані SMS | Логіка перевірена; повний польовий цикл — **ні** |
| Адресний фільтр SMS | 18 хостових перевірок пройдено; на залізі — **ні** |
| **RCTL (Radio Check)** | **На залізі не перевірявся взагалі** |
| S-метр, теми, гасла | Перевірено на живій рації |

## 19. Збірка з джерел

Повний опис — у **`BUILD.md`**: тулчейн, донор кодека, прапорці збірки
(`LANGUAGE_UKRAINIAN=1 ENABLE_AES=1 ENABLE_DMR_DATA=1`), автоматична збірка в CI,
доступ до USB на Linux/Windows/WSL2.

Швидка локальна перевірка синтаксису без ARM-тулчейну:
`MDUV380_firmware/tools/syntax_check.sh`.

---

## Подяки й ліцензія

Основа — **OpenGD77** від Roger Clark VK3KYY та учасників проєкту
(ліцензія в стилі BSD-3-clause, лише некомерційне використання). Цей форк зберігає
всі авторські заголовки апстріму.

Кодек AMBE+2 — власність DVSI, у прошивці його немає; він переноситься з донорської
прошивки під час прошивання.
